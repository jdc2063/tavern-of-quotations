installation105() {
    if [[ -z "$personnage101" ]]; then
        personnage101=$reponse
    else
        if [[ -z "$personnage102" ]]; then
            personnage102=$reponse
        else
            if [[ -z "$personnage103" ]]; then
                personnage103=$reponse
            else
                if [[ -z "$personnage104" ]]; then
                    personnage104=$reponse
                else
                    if [[ -z "$personnage105" ]]; then
                        personnage105=$reponse
                    else
                        echo "plus de place"
                    fi
                fi
            fi
        fi
    fi
}

installation100() {
    if [[ -z "$personnage96" ]]; then
        personnage96=$reponse
    else
        if [[ -z "$personnage97" ]]; then
            personnage97=$reponse
        else
            if [[ -z "$personnage98" ]]; then
                personnage98=$reponse
            else
                if [[ -z "$personnage99" ]]; then
                    personnage99=$reponse
                else
                    if [[ -z "$personnage10" ]]; then
                        personnage100=$reponse
                    else
                        installation105
                    fi
                fi
            fi
        fi
    fi
}

installation95() {
    if [[ -z "$personnage91" ]]; then
        personnage91=$reponse
    else
        if [[ -z "$personnage92" ]]; then
            personnage92=$reponse
        else
            if [[ -z "$personnage93" ]]; then
                personnage93=$reponse
            else
                if [[ -z "$personnage94" ]]; then
                    personnage94=$reponse
                else
                    if [[ -z "$personnage95" ]]; then
                        personnage95=$reponse
                    else
                        installation100
                    fi
                fi
            fi
        fi
    fi
}

installation90() {
    if [[ -z "$personnage86" ]]; then
        personnage86=$reponse
    else
        if [[ -z "$personnage87" ]]; then
            personnage87=$reponse
        else
            if [[ -z "$personnage88" ]]; then
                personnage88=$reponse
            else
                if [[ -z "$personnage89" ]]; then
                    personnage89=$reponse
                else
                    if [[ -z "$personnage90" ]]; then
                        personnage90=$reponse
                    else
                        installation95
                    fi
                fi
            fi
        fi
    fi
}

installation85() {
    if [[ -z "$personnage81" ]]; then
        personnage81=$reponse
    else
        if [[ -z "$personnage82" ]]; then
            personnage82=$reponse
        else
            if [[ -z "$personnage83" ]]; then
                personnage83=$reponse
            else
                if [[ -z "$personnage84" ]]; then
                    personnage84=$reponse
                else
                    if [[ -z "$personnage85" ]]; then
                        personnage85=$reponse
                    else
                        installation90
                    fi
                fi
            fi
        fi
    fi
}

installation80() {
    if [[ -z "$personnage76" ]]; then
        personnage76=$reponse
    else
        if [[ -z "$personnage77" ]]; then
            personnage77=$reponse
        else
            if [[ -z "$personnage78"  ]]; then
                personnage78=$reponse
            else
                if [[ -z "$personnage79" ]]; then
                    personnage79=$reponse
                else
                    if [[ -z "$personnage80" ]]; then
                        personnage80=$reponse
                    else
                        installation85
                    fi
                fi
            fi
        fi
    fi
}

installation75() {
    if [[ -z "$personnage71" ]]; then
        personnage71=$reponse
    else
        if [[ -z "$personnage72" ]]; then
            personnage72=$reponse
        else
            if [[ -z "$personnage73" ]]; then
                personnage73=$reponse
            else
                if [[ -z "$personnage74" ]]; then
                    personnage74=$reponse
                else
                    if [[ -z "$personnage75" ]]; then
                        personnage75=$reponse
                    else
                        installation80
                    fi
                fi
            fi
        fi
    fi
}

installation70() {
    if [[ -z "$personnage66" ]]; then
        personnage66=$reponse
    else
        if [[ -z "$personnage67" ]]; then
            personnage67=$reponse
        else
            if [[ -z "$personnage68" ]]; then
                personnage68=$reponse
            else
                if [[ -z "$personnage69" ]]; then
                    personnage69=$reponse
                else
                    if [[ -z "$personnage70" ]]; then
                        personnage70=$reponse
                    else
                        installation75
                    fi
                fi
            fi
        fi
    fi
}

installation65() {
    if [[ -z "$personnage61" ]]; then
        personnage61=$reponse
    else
        if [[ -z "$personnage62" ]]; then
            personnage62=$reponse
        else
            if [[ -z "$personnage63" ]]; then
                personnage63=$reponse
            else
                if [[ -z "$personnage64" ]]; then
                    personnage64=$reponse
                else
                    if [[ -z "$personnage65" ]]; then
                        personnage65=$reponse
                    else
                        installation70
                    fi
                fi
            fi
        fi
    fi
}

installation60() {
    if [[ -z "$personnage56" ]]; then
        personnage56=$reponse
    else
        if [[ -z "$personnage57" ]]; then
            personnage57=$reponse
        else
            if [[ -z "$personnage58" ]]; then
                personnage58=$reponse
            else
                if [[ -z "$personnage59" ]]; then
                    personnage59=$reponse
                else
                    if [[ -z "$personnage60" ]]; then
                        personnage60=$reponse
                    else
                        installation65
                    fi
                fi
            fi
        fi
    fi
}

installation55() {
    if [[ -z "$personnage51" ]]; then
        personnage51=$reponse
    else
        if [[ -z "$personnage52" ]]; then
            personnage52=$reponse
        else
            if [[ -z "$personnage53" ]]; then
                personnage53=$reponse
            else
                if [[ -z "$personnage54" ]]; then
                    personnage54=$reponse
                else
                    if [[ -z "$personnage55" ]]; then
                        personnage55=$reponse
                    else
                        installation60
                    fi
                fi
            fi
        fi
    fi
}

installation50() {
    if [[ -z "$personnage46" ]]; then
        personnage46=$reponse
    else
        if [[ -z "$personnage47" ]]; then
            personnage47=$reponse
        else
            if [[ -z "$personnage48" ]]; then
                personnage48=$reponse
            else
                if [[ -z "$personnage49" ]]; then
                    personnage49=$reponse
                else
                    if [[ -z "$personnage50" ]]; then
                        personnage50=$reponse
                    else
                        installation55
                    fi
                fi
            fi
        fi
    fi
}

installation45() {
    if [[ -z "$personnage41" ]]; then
        personnage41=$reponse
    else
        if [[ -z "$personnage42" ]]; then
            personnage42=$reponse
        else
            if [[ -z "$personnage43" ]]; then
                personnage43=$reponse
            else
                if [[ -z "$personnage44" ]]; then
                    personnage44=$reponse
                else
                    if [[ -z "$personnage45" ]]; then
                        personnage45=$reponse
                    else
                        installation50
                    fi
                fi
            fi
        fi
    fi
}

installation40() {
    if [[ -z "$personnage36" ]]; then
        personnage36=$reponse
    else
        if [[ -z "$personnage37" ]]; then
            personnage37=$reponse
        else
            if [[ -z "$personnage38" ]]; then
                personnage38=$reponse
            else
                if [[ -z "$personnage39" ]]; then
                    personnage39=$reponse
                else
                    if [[ -z "$personnage40" ]]; then
                        personnage40=$reponse
                    else
                        installation45
                    fi
                fi
            fi
        fi
    fi
}

installation35() {
    if [[ -z "$personnage31" ]]; then
        personnage31=$reponse
    else
        if [[ -z "$personnage32" ]]; then
            personnage32=$reponse
        else
            if [[ -z "$personnage33" ]]; then
                personnage33=$reponse
            else
                if [[ -z "$personnage34" ]]; then
                    personnage34=$reponse
                else
                    if [[ -z "$personnage35" ]]; then
                        personnage35=$reponse
                    else
                        installation40
                    fi
                fi
            fi
        fi
    fi
}

installation30() {
    if [[ -z "$personnage26" ]]; then
        personnage26=$reponse
    else
        if [[ -z "$personnage27" ]]; then
            personnage27=$reponse
        else
            if [[ -z "$personnage28" ]]; then
                personnage28=$reponse
            else
                if [[ -z "$personnage29" ]]; then
                    personnage29=$reponse
                else
                    if [[ -z "$personnage30" ]]; then
                        personnage30=$reponse
                    else
                        installation35
                    fi
                fi
            fi
        fi
    fi
}

installation25() {
    if [[ -z "$personnage21" ]]; then
        personnage21=$reponse
    else
        if [[ -z "$personnage22" ]]; then
            personnage22=$reponse
        else
            if [[ -z "$personnage23" ]]; then
                personnage23=$reponse
            else
                if [[ -z "$personnage24" ]]; then
                    personnage24=$reponse
                else
                    if [[ -z "$personnage25" ]]; then
                        personnage25=$reponse
                    else
                        installation30
                    fi
                fi
            fi
        fi
    fi
}

installation20() {
    if [[ -z "$personnage16" ]]; then
        personnage16=$reponse
    else
        if [[ -z "$personnage17" ]]; then
            personnage17=$reponse
        else
            if [[ -z "$personnage18" ]]; then
                personnage18=$reponse
            else
                if [[ -z "$personnage19" ]]; then
                    personnage19=$reponse
                else
                    if [[ -z "$personnage20" ]]; then
                        personnage20=$reponse
                    else
                        installation25
                    fi
                fi
            fi
        fi
    fi
}

installation15() {
    if [[ -z "$personnage11" ]]; then
        personnage11=$reponse
    else
        if [[ -z "$personnage12" ]]; then
            personnage12=$reponse
        else
            if [[ -z "$personnage13" ]]; then
                personnage13=$reponse
            else
                if [[ -z "$personnage14" ]]; then
                    personnage14=$reponse
                else
                    if [[ -z "$personnage15" ]]; then
                        personnage15=$reponse
                    else
                        installation20
                    fi
                fi
            fi
        fi
    fi
}

installation10() {
    if [[ -z "$personnage6" ]]; then
        personnage6=$reponse
    else
        if [[ -z "$personnage7" ]]; then
            personnage7=$reponse
        else
            if [[ -z "$personnage8" ]]; then
                personnage8=$reponse
            else
                if [[ -z "$personnage9" ]]; then
                    personnage9=$reponse
                else
                    if [[ -z "$personnage10" ]]; then
                        personnage10=$reponse
                    else
                        installation15
                    fi
                fi
            fi
        fi
    fi
}

installation5() {
    if [[ -z "$personnage1" ]]; then
        personnage1=$reponse
    else
        if [[ -z "$personnage2" ]]; then
            personnage2=$reponse
        else
            if [[ -z "$personnage3" ]]; then
                personnage3=$reponse
            else
                if [[ -z "$personnage4" ]]; then
                    personnage4=$reponse
                else
                    if [[ -z "$personnage5" ]]; then
                        personnage5=$reponse
                    else
                        installation10
                    fi
                fi
            fi
        fi
    fi
}

installation5
