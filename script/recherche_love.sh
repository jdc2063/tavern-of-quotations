recherche105() {
    if [[ $reponse != $personnage101 ]]; then
        if [[ $reponse != $personnage102 ]]; then
            if [[ $reponse != $personnage103 ]]; then
                if [[ $reponse != $personnage104 ]]; then
                    if [[ $reponse != $personnage105 ]]; then
                        exist=1
                    fi
                fi
            fi
        fi
    fi
}

recherche100() {
    if [[ $reponse != $personnage96 ]]; then
        if [[ $reponse != $personnage97 ]]; then
            if [[ $reponse != $personnage98 ]]; then
                if [[ $reponse != $personnage99 ]]; then
                    if [[ $reponse != $personnage100 ]]; then
                        recherche105
                    fi
                fi
            fi
        fi
    fi
}

recherche95() {
if [[ $reponse != $personnage91 ]]; then
    if [[ $reponse != $personnage92 ]]; then
        if [[ $reponse != $personnage93 ]]; then
            if [[ $reponse != $personnage94 ]]; then
                if [[ $reponse != $personnage95 ]]; then
                    recherche100
                fi
            fi
        fi
    fi
fi
}

recherche90() {
    if [[ $reponse != $personnage86 ]]; then
        if [[ $reponse != $personnage87 ]]; then
            if [[ $reponse != $personnage88 ]]; then
                if [[ $reponse != $personnage89 ]]; then
                    if [[ $reponse != $personnage90 ]]; then
                        recherche95
                    fi
                fi
            fi
        fi
    fi
}

recherche85() {
    if [[ $reponse != $personnage81 ]]; then
        if [[ $reponse != $personnage82 ]]; then
            if [[ $reponse != $personnage83 ]]; then
                if [[ $reponse != $personnage84 ]]; then
                    if [[ $reponse != $personnage85 ]]; then
                        recherche90
                    fi
                fi
            fi
        fi
    fi
}

recherche80() {
    if [[ $reponse != $personnage76 ]]; then
        if [[ $reponse != $personnage77 ]]; then
            if [[ $reponse != $personnage78 ]]; then
                if [[ $reponse != $personnage79 ]]; then
                    if [[ $reponse != $personnage80 ]]; then
                        recherche85
                    fi
                fi
            fi
        fi
    fi
}

recherche75() {
if [[ $reponse != $personnage71 ]]; then
    if [[ $reponse != $personnage72 ]]; then
        if [[ $reponse != $personnage73 ]]; then
            if [[ $reponse != $personnage74 ]]; then
                if [[ $reponse != $personnage75 ]]; then
                    recherche80
                fi
            fi
        fi
    fi
fi
}

recherche70() {
    if [[ $reponse != $personnage66 ]]; then
        if [[ $reponse != $personnage67 ]]; then
            if [[ $reponse != $personnage68 ]]; then
                if [[ $reponse != $personnage69 ]]; then
                    if [[ $reponse != $personnage70 ]]; then
                        recherche75
                    fi
                fi
            fi
        fi
    fi
}

recherche65() {
    if [[ $reponse != $personnage61 ]]; then
        if [[ $reponse != $personnage62 ]]; then
            if [[ $reponse != $personnage63 ]]; then
                if [[ $reponse != $personnage64 ]]; then
                    if [[ $reponse != $personnage65 ]]; then
                        recherche70
                    fi
                fi
            fi
        fi
    fi
}

recherche60() {
    if [[ $reponse != $personnage56 ]]; then
        if [[ $reponse != $personnage57 ]]; then
            if [[ $reponse != $personnage58 ]]; then
                if [[ $reponse != $personnage59 ]]; then
                    if [[ $reponse != $personnage60 ]]; then
                        recherche65
                    fi
                fi
            fi
        fi
    fi
}

recherche55() {
    if [[ $reponse != $personnage51 ]]; then
        if [[ $reponse != $personnage52 ]]; then
            if [[ $reponse != $personnage53 ]]; then
                if [[ $reponse != $personnage54 ]]; then
                    if [[ $reponse != $personnage55 ]]; then
                        recherche60
                    fi
                fi
            fi
        fi
    fi
}

recherche50() {
    if [[ $reponse != $personnage46 ]]; then
        if [[ $reponse != $personnage47 ]]; then
            if [[ $reponse != $personnage48 ]]; then
                if [[ $reponse != $personnage49 ]]; then
                    if [[ $reponse != $personnage50 ]]; then
                        recherche55
                    fi
                fi
            fi
        fi
    fi
}

recherche45() {
    if [[ $reponse != $personnage41 ]]; then
        if [[ $reponse != $personnage42 ]]; then
            if [[ $reponse != $personnage43 ]]; then
                if [[ $reponse != $personnage44 ]]; then
                    if [[ $reponse != $personnage45 ]]; then
                        recherche50
                    fi
                fi
            fi
        fi
    fi
}

recherche40() {
    if [[ $reponse != $personnage36 ]]; then
        if [[ $reponse != $personnage37 ]]; then
            if [[ $reponse != $personnage38 ]]; then
                if [[ $reponse != $personnage39 ]]; then
                    if [[ $reponse != $personnage40 ]]; then
                        recherche45
                    fi
                fi
            fi
        fi
    fi
}

recherche35() {
    if [[ $reponse != $personnage31 ]]; then
        if [[ $reponse != $personnage32 ]]; then
            if [[ $reponse != $personnage33 ]]; then
                if [[ $reponse != $personnage34 ]]; then
                    if [[ $reponse != $personnage35 ]]; then
                        recherche40
                    fi
                fi
            fi
        fi
    fi
}

recherche30() {
    if [[ $reponse != $personnage26 ]]; then
        if [[ $reponse != $personnage27 ]]; then
            if [[ $reponse != $personnage28 ]]; then
                if [[ $reponse != $personnage29 ]]; then
                    if [[ $reponse != $personnage30 ]]; then
                        recherche35
                    fi
                fi
            fi
        fi
    fi
}

recherche25() {
    if [[ $reponse != $personnage21 ]]; then
        if [[ $reponse != $personnage22 ]]; then
            if [[ $reponse != $personnage23 ]]; then
                if [[ $reponse != $personnage24 ]]; then
                    if [[ $reponse != $personnage25 ]]; then
                        recherche30
                    fi
                fi
            fi
        fi
    fi
}

recherche20() {
    if [[ $reponse != $personnage16 ]]; then
        if [[ $reponse != $personnage17 ]]; then
            if [[ $reponse != $personnage18 ]]; then
                if [[ $reponse != $personnage19 ]]; then
                    if [[ $reponse != $personnage20 ]]; then
                        recherche25
                    fi
                fi
            fi
        fi
    fi
}

recherche15() {
    if [[ $reponse != $personnage11 ]]; then
        if [[ $reponse != $personnage12 ]]; then
            if [[ $reponse != $personnage13 ]]; then
                if [[ $reponse != $personnage14 ]]; then
                    if [[ $reponse != $personnage15 ]]; then
                        recherche20
                    fi
                fi
            fi
        fi
    fi
}

recherche10() {
    if [[ $reponse != $personnage6 ]]; then
        if [[ $reponse != $personnage7 ]]; then
            if [[ $reponse != $personnage8 ]]; then
                if [[ $reponse != $personnage9 ]]; then
                    if [[ $reponse != $personnage10 ]]; then
                        recherche15
                    fi
                fi
            fi
        fi
    fi
}

recherche5() {
    if [[ $reponse != $personnage1 ]]; then
        if [[ $reponse != $personnage2 ]]; then
            if [[ $reponse != $personnage3 ]]; then
                if [[ $reponse != $personnage4 ]]; then
                    if [[ $reponse != $personnage5 ]]; then
                        recherche10
                    fi
                fi
            fi
        fi
    fi
}

exist=0
recherche5

