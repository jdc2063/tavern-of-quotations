recherche105() {
    if [[ $ran_a -ne $nombre101 ]]; then
        if [[ $ran_a -ne $nombre102 ]]; then
            if [[ $ran_a -ne $nombre103 ]]; then
                if [[ $ran_a -ne $nombre104 ]]; then
                    if [[ $ran_a -ne $nombre105 ]]; then
                        exist=1
                    fi
                fi
            fi
        fi
    fi
}

recherche100() {
    if [[ $ran_a -ne $nombre96 ]]; then
        if [[ $ran_a -ne $nombre97 ]]; then
            if [[ $ran_a -ne $nombre98 ]]; then
                if [[ $ran_a -ne $nombre99 ]]; then
                    if [[ $ran_a -ne $nombre100 ]]; then
                        recherche105
                    fi
                fi
            fi
        fi
    fi
}

recherche95() {
if [[ $ran_a -ne $nombre91 ]]; then
    if [[ $ran_a -ne $nombre92 ]]; then
        if [[ $ran_a -ne $nombre93 ]]; then
            if [[ $ran_a -ne $nombre94 ]]; then
                if [[ $ran_a -ne $nombre95 ]]; then
                    recherche100
                fi
            fi
        fi
    fi
fi
}

recherche90() {
    if [[ $ran_a -ne $nombre86 ]]; then
        if [[ $ran_a -ne $nombre87 ]]; then
            if [[ $ran_a -ne $nombre88 ]]; then
                if [[ $ran_a -ne $nombre89 ]]; then
                    if [[ $ran_a -ne $nombre90 ]]; then
                        recherche95
                    fi
                fi
            fi
        fi
    fi
}

recherche85() {
    if [[ $ran_a -ne $nombre81 ]]; then
        if [[ $ran_a -ne $nombre82 ]]; then
            if [[ $ran_a -ne $nombre83 ]]; then
                if [[ $ran_a -ne $nombre84 ]]; then
                    if [[ $ran_a -ne $nombre85 ]]; then
                        recherche90
                    fi
                fi
            fi
        fi
    fi
}

recherche80() {
    if [[ $ran_a -ne $nombre76 ]]; then
        if [[ $ran_a -ne $nombre77 ]]; then
            if [[ $ran_a -ne $nombre78 ]]; then
                if [[ $ran_a -ne $nombre79 ]]; then
                    if [[ $ran_a -ne $nombre80 ]]; then
                        recherche85
                    fi
                fi
            fi
        fi
    fi
}

recherche75() {
if [[ $ran_a -ne $nombre71 ]]; then
    if [[ $ran_a -ne $nombre72 ]]; then
        if [[ $ran_a -ne $nombre73 ]]; then
            if [[ $ran_a -ne $nombre74 ]]; then
                if [[ $ran_a -ne $nombre75 ]]; then
                    recherche80
                fi
            fi
        fi
    fi
fi
}

recherche70() {
    if [[ $ran_a -ne $nombre66 ]]; then
        if [[ $ran_a -ne $nombre67 ]]; then
            if [[ $ran_a -ne $nombre68 ]]; then
                if [[ $ran_a -ne $nombre69 ]]; then
                    if [[ $ran_a -ne $nombre70 ]]; then
                        recherche75
                    fi
                fi
            fi
        fi
    fi
}

recherche65() {
    if [[ $ran_a -ne $nombre61 ]]; then
        if [[ $ran_a -ne $nombre62 ]]; then
            if [[ $ran_a -ne $nombre63 ]]; then
                if [[ $ran_a -ne $nombre64 ]]; then
                    if [[ $ran_a -ne $nombre65 ]]; then
                        recherche70
                    fi
                fi
            fi
        fi
    fi
}

recherche60() {
    if [[ $ran_a -ne $nombre56 ]]; then
        if [[ $ran_a -ne $nombre57 ]]; then
            if [[ $ran_a -ne $nombre58 ]]; then
                if [[ $ran_a -ne $nombre59 ]]; then
                    if [[ $ran_a -ne $nombre60 ]]; then
                        recherche65
                    fi
                fi
            fi
        fi
    fi
}

recherche55() {
    if [[ $ran_a -ne $nombre51 ]]; then
        if [[ $ran_a -ne $nombre52 ]]; then
            if [[ $ran_a -ne $nombre53 ]]; then
                if [[ $ran_a -ne $nombre54 ]]; then
                    if [[ $ran_a -ne $nombre55 ]]; then
                        recherche60
                    fi
                fi
            fi
        fi
    fi
}

recherche50() {
    if [[ $ran_a -ne $nombre46 ]]; then
        if [[ $ran_a -ne $nombre47 ]]; then
            if [[ $ran_a -ne $nombre48 ]]; then
                if [[ $ran_a -ne $nombre49 ]]; then
                    if [[ $ran_a -ne $nombre50 ]]; then
                        recherche55
                    fi
                fi
            fi
        fi
    fi
}

recherche45() {
    if [[ $ran_a -ne $nombre41 ]]; then
        if [[ $ran_a -ne $nombre42 ]]; then
            if [[ $ran_a -ne $nombre43 ]]; then
                if [[ $ran_a -ne $nombre44 ]]; then
                    if [[ $ran_a -ne $nombre45 ]]; then
                        recherche50
                    fi
                fi
            fi
        fi
    fi
}

recherche40() {
    if [[ $ran_a -ne $nombre36 ]]; then
        if [[ $ran_a -ne $nombre37 ]]; then
            if [[ $ran_a -ne $nombre38 ]]; then
                if [[ $ran_a -ne $nombre39 ]]; then
                    if [[ $ran_a -ne $nombre40 ]]; then
                        recherche45
                    fi
                fi
            fi
        fi
    fi
}

recherche35() {
    if [[ $ran_a -ne $nombre31 ]]; then
        if [[ $ran_a -ne $nombre32 ]]; then
            if [[ $ran_a -ne $nombre33 ]]; then
                if [[ $ran_a -ne $nombre34 ]]; then
                    if [[ $ran_a -ne $nombre35 ]]; then
                        recherche40
                    fi
                fi
            fi
        fi
    fi
}

recherche30() {
    if [[ $ran_a -ne $nombre26 ]]; then
        if [[ $ran_a -ne $nombre27 ]]; then
            if [[ $ran_a -ne $nombre28 ]]; then
                if [[ $ran_a -ne $nombre29 ]]; then
                    if [[ $ran_a -ne $nombre30 ]]; then
                        recherche35
                    fi
                fi
            fi
        fi
    fi
}

recherche25() {
    if [[ $ran_a -ne $nombre21 ]]; then
        if [[ $ran_a -ne $nombre22 ]]; then
            if [[ $ran_a -ne $nombre23 ]]; then
                if [[ $ran_a -ne $nombre24 ]]; then
                    if [[ $ran_a -ne $nombre25 ]]; then
                        recherche30
                    fi
                fi
            fi
        fi
    fi
}

recherche20() {
    if [[ $ran_a -ne $nombre16 ]]; then
        if [[ $ran_a -ne $nombre17 ]]; then
            if [[ $ran_a -ne $nombre18 ]]; then
                if [[ $ran_a -ne $nombre19 ]]; then
                    if [[ $ran_a -ne $nombre20 ]]; then
                        recherche25
                    fi
                fi
            fi
        fi
    fi
}

recherche15() {
    if [[ $ran_a -ne $nombre11 ]]; then
        if [[ $ran_a -ne $nombre12 ]]; then
            if [[ $ran_a -ne $nombre13 ]]; then
                if [[ $ran_a -ne $nombre14 ]]; then
                    if [[ $ran_a -ne $nombre15 ]]; then
                        recherche20
                    fi
                fi
            fi
        fi
    fi
}

recherche10() {
    if [[ $ran_a -ne $nombre6 ]]; then
        if [[ $ran_a -ne $nombre7 ]]; then
            if [[ $ran_a -ne $nombre8 ]]; then
                if [[ $ran_a -ne $nombre9 ]]; then
                    if [[ $ran_a -ne $nombre10 ]]; then
                        recherche15
                    fi
                fi
            fi
        fi
    fi
}

recherche5() {
    if [[ $ran_a -ne $nombre1 ]]; then
        if [[ $ran_a -ne $nombre2 ]]; then
            if [[ $ran_a -ne $nombre3 ]]; then
                if [[ $ran_a -ne $nombre4 ]]; then
                    if [[ $ran_a -ne $nombre5 ]]; then
                        recherche10
                    fi
                fi
            fi
        fi
    fi
}

exist=0
recherche5

