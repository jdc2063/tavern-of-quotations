recherche300() {
    if [[ $ran_a -ne $nombre296 || $arc_actuel -ne $arc296 ]]; then
        if [[ $ran_a -ne $nombre297 || $arc_actuel -ne $arc297 ]]; then
            if [[ $ran_a -ne $nombre298 || $arc_actuel -ne $arc298 ]]; then
                if [[ $ran_a -ne $nombre299 || $arc_actuel -ne $arc299 ]]; then
                    if [[ $ran_a -ne $nombre300 || $arc_actuel -ne $arc300 ]]; then
                        exist=1
                    fi
                fi
            fi
        fi
    fi
}

recherche295() {
    if [[ $ran_a -ne $nombre291 || $arc_actuel -ne $arc291 ]]; then
        if [[ $ran_a -ne $nombre292 || $arc_actuel -ne $arc292 ]]; then
            if [[ $ran_a -ne $nombre293 || $arc_actuel -ne $arc293 ]]; then
                if [[ $ran_a -ne $nombre294 || $arc_actuel -ne $arc294 ]]; then
                    if [[ $ran_a -ne $nombre295 || $arc_actuel -ne $arc295 ]]; then
                        recherche300
                    fi
                fi
            fi
        fi
    fi
}

recherche290() {
    if [[ $ran_a -ne $nombre286 || $arc_actuel -ne $arc286 ]]; then
        if [[ $ran_a -ne $nombre287 || $arc_actuel -ne $arc287 ]]; then
            if [[ $ran_a -ne $nombre288 || $arc_actuel -ne $arc288 ]]; then
                if [[ $ran_a -ne $nombre289 || $arc_actuel -ne $arc289 ]]; then
                    if [[ $ran_a -ne $nombre290 || $arc_actuel -ne $arc290 ]]; then
                        recherche295
                    fi
                fi
            fi
        fi
    fi
}

recherche285() {
    if [[ $ran_a -ne $nombre281 || $arc_actuel -ne $arc281 ]]; then
        if [[ $ran_a -ne $nombre282 || $arc_actuel -ne $arc282 ]]; then
            if [[ $ran_a -ne $nombre283 || $arc_actuel -ne $arc283 ]]; then
                if [[ $ran_a -ne $nombre284 || $arc_actuel -ne $arc284 ]]; then
                    if [[ $ran_a -ne $nombre285 || $arc_actuel -ne $arc285 ]]; then
                        recherche290
                    fi
                fi
            fi
        fi
    fi
}

recherche280() {
    if [[ $ran_a -ne $nombre276 || $arc_actuel -ne $arc276 ]]; then
        if [[ $ran_a -ne $nombre277 || $arc_actuel -ne $arc277 ]]; then
            if [[ $ran_a -ne $nombre278 || $arc_actuel -ne $arc278 ]]; then
                if [[ $ran_a -ne $nombre279 || $arc_actuel -ne $arc279 ]]; then
                    if [[ $ran_a -ne $nombre280 || $arc_actuel -ne $arc280 ]]; then
                        recherche285
                    fi
                fi
            fi
        fi
    fi
}

recherche275() {
    if [[ $ran_a -ne $nombre271 || $arc_actuel -ne $arc271 ]]; then
        if [[ $ran_a -ne $nombre272 || $arc_actuel -ne $arc272 ]]; then
            if [[ $ran_a -ne $nombre273 || $arc_actuel -ne $arc273 ]]; then
                if [[ $ran_a -ne $nombre274 || $arc_actuel -ne $arc274 ]]; then
                    if [[ $ran_a -ne $nombre275 || $arc_actuel -ne $arc275 ]]; then
                        recherche280
                    fi
                fi
            fi
        fi
    fi
}

recherche270() {
    if [[ $ran_a -ne $nombre266 || $arc_actuel -ne $arc266 ]]; then
        if [[ $ran_a -ne $nombre267 || $arc_actuel -ne $arc267 ]]; then
            if [[ $ran_a -ne $nombre268 || $arc_actuel -ne $arc268 ]]; then
                if [[ $ran_a -ne $nombre269 || $arc_actuel -ne $arc269 ]]; then
                    if [[ $ran_a -ne $nombre270 || $arc_actuel -ne $arc270 ]]; then
                        recherche275
                    fi
                fi
            fi
        fi
    fi
}

recherche265() {
    if [[ $ran_a -ne $nombre261 || $arc_actuel -ne $arc261 ]]; then
        if [[ $ran_a -ne $nombre262 || $arc_actuel -ne $arc262 ]]; then
            if [[ $ran_a -ne $nombre263 || $arc_actuel -ne $arc263 ]]; then
                if [[ $ran_a -ne $nombre264 || $arc_actuel -ne $arc264 ]]; then
                    if [[ $ran_a -ne $nombre265 || $arc_actuel -ne $arc265 ]]; then
                        recherche270
                    fi
                fi
            fi
        fi
    fi
}

recherche260() {
    if [[ $ran_a -ne $nombre256 || $arc_actuel -ne $arc256 ]]; then
        if [[ $ran_a -ne $nombre257 || $arc_actuel -ne $arc257 ]]; then
            if [[ $ran_a -ne $nombre258 || $arc_actuel -ne $arc258 ]]; then
                if [[ $ran_a -ne $nombre259 || $arc_actuel -ne $arc259 ]]; then
                    if [[ $ran_a -ne $nombre260 || $arc_actuel -ne $arc260 ]]; then
                        recherche265
                    fi
                fi
            fi
        fi
    fi
}

recherche255() {
    if [[ $ran_a -ne $nombre251 || $arc_actuel -ne $arc251 ]]; then
        if [[ $ran_a -ne $nombre252 || $arc_actuel -ne $arc252 ]]; then
            if [[ $ran_a -ne $nombre253 || $arc_actuel -ne $arc253 ]]; then
                if [[ $ran_a -ne $nombre254 || $arc_actuel -ne $arc254 ]]; then
                    if [[ $ran_a -ne $nombre255 || $arc_actuel -ne $arc255 ]]; then
                        recherche260
                    fi
                fi
            fi
        fi
    fi
}

recherche250() {
    if [[ $ran_a -ne $nombre246 || $arc_actuel -ne $arc246 ]]; then
        if [[ $ran_a -ne $nombre247 || $arc_actuel -ne $arc247 ]]; then
            if [[ $ran_a -ne $nombre248 || $arc_actuel -ne $arc248 ]]; then
                if [[ $ran_a -ne $nombre249 || $arc_actuel -ne $arc249 ]]; then
                    if [[ $ran_a -ne $nombre250 || $arc_actuel -ne $arc250 ]]; then
                        recherche255
                    fi
                fi
            fi
        fi
    fi
}

recherche245() {
    if [[ $ran_a -ne $nombre241 || $arc_actuel -ne $arc241 ]]; then
        if [[ $ran_a -ne $nombre242 || $arc_actuel -ne $arc242 ]]; then
            if [[ $ran_a -ne $nombre243 || $arc_actuel -ne $arc243 ]]; then
                if [[ $ran_a -ne $nombre244 || $arc_actuel -ne $arc244 ]]; then
                    if [[ $ran_a -ne $nombre245 || $arc_actuel -ne $arc245 ]]; then
                        recherche250
                    fi
                fi
            fi
        fi
    fi
}

recherche240() {
    if [[ $ran_a -ne $nombre236 || $arc_actuel -ne $arc236 ]]; then
        if [[ $ran_a -ne $nombre237 || $arc_actuel -ne $arc237 ]]; then
            if [[ $ran_a -ne $nombre238 || $arc_actuel -ne $arc238 ]]; then
                if [[ $ran_a -ne $nombre239 || $arc_actuel -ne $arc239 ]]; then
                    if [[ $ran_a -ne $nombre240 || $arc_actuel -ne $arc240 ]]; then
                        recherche245
                    fi
                fi
            fi
        fi
    fi
}

recherche235() {
    if [[ $ran_a -ne $nombre231 || $arc_actuel -ne $arc231 ]]; then
        if [[ $ran_a -ne $nombre232 || $arc_actuel -ne $arc232 ]]; then
            if [[ $ran_a -ne $nombre233 || $arc_actuel -ne $arc233 ]]; then
                if [[ $ran_a -ne $nombre234 || $arc_actuel -ne $arc234 ]]; then
                    if [[ $ran_a -ne $nombre235 || $arc_actuel -ne $arc235 ]]; then
                        recherche240
                    fi
                fi
            fi
        fi
    fi
}

recherche230() {
    if [[ $ran_a -ne $nombre226 || $arc_actuel -ne $arc226 ]]; then
        if [[ $ran_a -ne $nombre227 || $arc_actuel -ne $arc227 ]]; then
            if [[ $ran_a -ne $nombre228 || $arc_actuel -ne $arc228 ]]; then
                if [[ $ran_a -ne $nombre229 || $arc_actuel -ne $arc229 ]]; then
                    if [[ $ran_a -ne $nombre230 || $arc_actuel -ne $arc230 ]]; then
                        recherche235
                    fi
                fi
            fi
        fi
    fi
}

recherche225() {
    if [[ $ran_a -ne $nombre221 || $arc_actuel -ne $arc221 ]]; then
        if [[ $ran_a -ne $nombre222 || $arc_actuel -ne $arc222 ]]; then
            if [[ $ran_a -ne $nombre223 || $arc_actuel -ne $arc223 ]]; then
                if [[ $ran_a -ne $nombre224 || $arc_actuel -ne $arc224 ]]; then
                    if [[ $ran_a -ne $nombre225 || $arc_actuel -ne $arc225 ]]; then
                        recherche230
                    fi
                fi
            fi
        fi
    fi
}

recherche220() {
    if [[ $ran_a -ne $nombre216 || $arc_actuel -ne $arc216 ]]; then
        if [[ $ran_a -ne $nombre217 || $arc_actuel -ne $arc217 ]]; then
            if [[ $ran_a -ne $nombre218 || $arc_actuel -ne $arc218 ]]; then
                if [[ $ran_a -ne $nombre219 || $arc_actuel -ne $arc219 ]]; then
                    if [[ $ran_a -ne $nombre220 || $arc_actuel -ne $arc220 ]]; then
                        recherche225
                    fi
                fi
            fi
        fi
    fi
}

recherche215() {
    if [[ $ran_a -ne $nombre211 || $arc_actuel -ne $arc211 ]]; then
        if [[ $ran_a -ne $nombre212 || $arc_actuel -ne $arc212 ]]; then
            if [[ $ran_a -ne $nombre213 || $arc_actuel -ne $arc213 ]]; then
                if [[ $ran_a -ne $nombre214 || $arc_actuel -ne $arc214 ]]; then
                    if [[ $ran_a -ne $nombre215 || $arc_actuel -ne $arc215 ]]; then
                        recherche220
                    fi
                fi
            fi
        fi
    fi
}

recherche210() {
    if [[ $ran_a -ne $nombre206 || $arc_actuel -ne $arc206 ]]; then
        if [[ $ran_a -ne $nombre207A || $arc_actuel -ne $arc207 ]]; then
            if [[ $ran_a -ne $nombre208A || $arc_actuel -ne $arc208 ]]; then
                if [[ $ran_a -ne $nombre209 || $arc_actuel -ne $arc209 ]]; then
                    if [[ $ran_a -ne $nombre210 || $arc_actuel -ne $arc210 ]]; then
                        recherche215
                    fi
                fi
            fi
        fi
    fi
}

recherche205() {
    if [[ $ran_a -ne $nombre201 || $arc_actuel -ne $arc201 ]]; then
        if [[ $ran_a -ne $nombre202 || $arc_actuel -ne $arc202 ]]; then
            if [[ $ran_a -ne $nombre203 || $arc_actuel -ne $arc203 ]]; then
                if [[ $ran_a -ne $nombre204 || $arc_actuel -ne $arc204 ]]; then
                    if [[ $ran_a -ne $nombre205 || $arc_actuel -ne $arc205 ]]; then
                        recherche210
                    fi
                fi
            fi
        fi
    fi
}

recherche200() {
    if [[ $ran_a -ne $nombre196 || $arc_actuel -ne $arc196 ]]; then
        if [[ $ran_a -ne $nombre197 || $arc_actuel -ne $arc197 ]]; then
            if [[ $ran_a -ne $nombre198 || $arc_actuel -ne $arc198 ]]; then
                if [[ $ran_a -ne $nombre199 || $arc_actuel -ne $arc199 ]]; then
                    if [[ $ran_a -ne $nombre200 || $arc_actuel -ne $arc200 ]]; then
                        recherche205
                    fi
                fi
            fi
        fi
    fi
}

recherche195() {
    if [[ $ran_a -ne $nombre191 || $arc_actuel -ne $arc191 ]]; then
        if [[ $ran_a -ne $nombre192 || $arc_actuel -ne $arc192 ]]; then
            if [[ $ran_a -ne $nombre193 || $arc_actuel -ne $arc193 ]]; then
                if [[ $ran_a -ne $nombre194 || $arc_actuel -ne $arc194 ]]; then
                    if [[ $ran_a -ne $nombre195 || $arc_actuel -ne $arc195 ]]; then
                        recherche200
                    fi
                fi
            fi
        fi
    fi
}

recherche190() {
    if [[ $ran_a -ne $nombre186 || $arc_actuel -ne $arc186 ]]; then
        if [[ $ran_a -ne $nombre187 || $arc_actuel -ne $arc187 ]]; then
            if [[ $ran_a -ne $nombre188 || $arc_actuel -ne $arc188 ]]; then
                if [[ $ran_a -ne $nombre189 || $arc_actuel -ne $arc189 ]]; then
                    if [[ $ran_a -ne $nombre190 || $arc_actuel -ne $arc190 ]]; then
                        recherche195
                    fi
                fi
            fi
        fi
    fi
}

recherche185() {
    if [[ $ran_a -ne $nombre181 || $arc_actuel -ne $arc181 ]]; then
        if [[ $ran_a -ne $nombre182 || $arc_actuel -ne $arc182 ]]; then
            if [[ $ran_a -ne $nombre183 || $arc_actuel -ne $arc183 ]]; then
                if [[ $ran_a -ne $nombre184 || $arc_actuel -ne $arc184 ]]; then
                    if [[ $ran_a -ne $nombre185 || $arc_actuel -ne $arc185 ]]; then
                        recherche190
                    fi
                fi
            fi
        fi
    fi
}

recherche180() {
    if [[ $ran_a -ne $nombre176 || $arc_actuel -ne $arc176 ]]; then
        if [[ $ran_a -ne $nombre177 || $arc_actuel -ne $arc177 ]]; then
            if [[ $ran_a -ne $nombre178 || $arc_actuel -ne $arc178 ]]; then
                if [[ $ran_a -ne $nombre179 || $arc_actuel -ne $arc179 ]]; then
                    if [[ $ran_a -ne $nombre180 || $arc_actuel -ne $arc180 ]]; then
                        recherche185
                    fi
                fi
            fi
        fi
    fi
}

recherche175() {
    if [[ $ran_a -ne $nombre171 || $arc_actuel -ne $arc171 ]]; then
        if [[ $ran_a -ne $nombre172 || $arc_actuel -ne $arc172 ]]; then
            if [[ $ran_a -ne $nombre173 || $arc_actuel -ne $arc173 ]]; then
                if [[ $ran_a -ne $nombre174 || $arc_actuel -ne $arc174 ]]; then
                    if [[ $ran_a -ne $nombre175 || $arc_actuel -ne $arc175 ]]; then
                        recherche180
                    fi
                fi
            fi
        fi
    fi
}

recherche170() {
    if [[ $ran_a -ne $nombre166 || $arc_actuel -ne $arc166 ]]; then
        if [[ $ran_a -ne $nombre167 || $arc_actuel -ne $arc167 ]]; then
            if [[ $ran_a -ne $nombre168 || $arc_actuel -ne $arc168 ]]; then
                if [[ $ran_a -ne $nombre169 || $arc_actuel -ne $arc169 ]]; then
                    if [[ $ran_a -ne $nombre170 || $arc_actuel -ne $arc170 ]]; then
                        recherche175
                    fi
                fi
            fi
        fi
    fi
}

recherche165() {
    if [[ $ran_a -ne $nombre161 || $arc_actuel -ne $arc161 ]]; then
        if [[ $ran_a -ne $nombre162 || $arc_actuel -ne $arc162 ]]; then
            if [[ $ran_a -ne $nombre163 || $arc_actuel -ne $arc163 ]]; then
                if [[ $ran_a -ne $nombre164 || $arc_actuel -ne $arc164 ]]; then
                    if [[ $ran_a -ne $nombre165 || $arc_actuel -ne $arc165 ]]; then
                        recherche170
                    fi
                fi
            fi
        fi
    fi
}

recherche160() {
    if [[ $ran_a -ne $nombre156 || $arc_actuel -ne $arc156 ]]; then
        if [[ $ran_a -ne $nombre157 || $arc_actuel -ne $arc157 ]]; then
            if [[ $ran_a -ne $nombre158 || $arc_actuel -ne $arc158 ]]; then
                if [[ $ran_a -ne $nombre159 || $arc_actuel -ne $arc159 ]]; then
                    if [[ $ran_a -ne $nombre160 || $arc_actuel -ne $arc160 ]]; then
                        recherche165
                    fi
                fi
            fi
        fi
    fi
}

recherche155() {
    if [[ $ran_a -ne $nombre151 || $arc_actuel -ne $arc151 ]]; then
        if [[ $ran_a -ne $nombre152 || $arc_actuel -ne $arc152 ]]; then
            if [[ $ran_a -ne $nombre153 || $arc_actuel -ne $arc153 ]]; then
                if [[ $ran_a -ne $nombre154 || $arc_actuel -ne $arc154 ]]; then
                    if [[ $ran_a -ne $nombre155 || $arc_actuel -ne $arc155 ]]; then
                        recherche160
                    fi
                fi
            fi
        fi
    fi
}

recherche150() {
    if [[ $ran_a -ne $nombre146 || $arc_actuel -ne $arc146 ]]; then
        if [[ $ran_a -ne $nombre147 || $arc_actuel -ne $arc147 ]]; then
            if [[ $ran_a -ne $nombre148 || $arc_actuel -ne $arc148 ]]; then
                if [[ $ran_a -ne $nombre149 || $arc_actuel -ne $arc149 ]]; then
                    if [[ $ran_a -ne $nombre150 || $arc_actuel -ne $arc150 ]]; then
                        recherche155
                    fi
                fi
            fi
        fi
    fi
}

recherche145() {
    if [[ $ran_a -ne $nombre141 || $arc_actuel -ne $arc141 ]]; then
        if [[ $ran_a -ne $nombre142 || $arc_actuel -ne $arc142 ]]; then
            if [[ $ran_a -ne $nombre143 || $arc_actuel -ne $arc143 ]]; then
                if [[ $ran_a -ne $nombre144 || $arc_actuel -ne $arc144 ]]; then
                    if [[ $ran_a -ne $nombre145 || $arc_actuel -ne $arc145 ]]; then
                        recherche150
                    fi
                fi
            fi
        fi
    fi
}

recherche140() {
    if [[ $ran_a -ne $nombre136 || $arc_actuel -ne $arc136 ]]; then
        if [[ $ran_a -ne $nombre137 || $arc_actuel -ne $arc137 ]]; then
            if [[ $ran_a -ne $nombre138 || $arc_actuel -ne $arc138 ]]; then
                if [[ $ran_a -ne $nombre139 || $arc_actuel -ne $arc139 ]]; then
                    if [[ $ran_a -ne $nombre140 || $arc_actuel -ne $arc140 ]]; then
                        recherche145
                    fi
                fi
            fi
        fi
    fi
}

recherche135() {
    if [[ $ran_a -ne $nombre131 || $arc_actuel -ne $arc131 ]]; then
        if [[ $ran_a -ne $nombre132 || $arc_actuel -ne $arc132 ]]; then
            if [[ $ran_a -ne $nombre133 || $arc_actuel -ne $arc133 ]]; then
                if [[ $ran_a -ne $nombre134 || $arc_actuel -ne $arc134 ]]; then
                    if [[ $ran_a -ne $nombre135 || $arc_actuel -ne $arc135 ]]; then
                        recherche140
                    fi
                fi
            fi
        fi
    fi
}

recherche130() {
    if [[ $ran_a -ne $nombre126 || $arc_actuel -ne $arc126 ]]; then
        if [[ $ran_a -ne $nombre127 || $arc_actuel -ne $arc127 ]]; then
            if [[ $ran_a -ne $nombre128 || $arc_actuel -ne $arc128 ]]; then
                if [[ $ran_a -ne $nombre129 || $arc_actuel -ne $arc129 ]]; then
                    if [[ $ran_a -ne $nombre130 || $arc_actuel -ne $arc130 ]]; then
                        recherche135
                    fi
                fi
            fi
        fi
    fi
}

recherche125() {
    if [[ $ran_a -ne $nombre121 || $arc_actuel -ne $arc121 ]]; then
        if [[ $ran_a -ne $nombre122 || $arc_actuel -ne $arc122 ]]; then
            if [[ $ran_a -ne $nombre123 || $arc_actuel -ne $arc123 ]]; then
                if [[ $ran_a -ne $nombre124 || $arc_actuel -ne $arc124 ]]; then
                    if [[ $ran_a -ne $nombre125 || $arc_actuel -ne $arc125 ]]; then
                        recherche130
                    fi
                fi
            fi
        fi
    fi
}

recherche120() {
    if [[ $ran_a -ne $nombre116 || $arc_actuel -ne $arc116 ]]; then
        if [[ $ran_a -ne $nombre117 || $arc_actuel -ne $arc117 ]]; then
            if [[ $ran_a -ne $nombre118 || $arc_actuel -ne $arc118 ]]; then
                if [[ $ran_a -ne $nombre119 || $arc_actuel -ne $arc119 ]]; then
                    if [[ $ran_a -ne $nombre120 || $arc_actuel -ne $arc120 ]]; then
                        recherche125
                    fi
                fi
            fi
        fi
    fi
}

recherche115() {
    if [[ $ran_a -ne $nombre111 || $arc_actuel -ne $arc111 ]]; then
        if [[ $ran_a -ne $nombre112 || $arc_actuel -ne $arc112 ]]; then
            if [[ $ran_a -ne $nombre113 || $arc_actuel -ne $arc113 ]]; then
                if [[ $ran_a -ne $nombre114 || $arc_actuel -ne $arc114 ]]; then
                    if [[ $ran_a -ne $nombre115 || $arc_actuel -ne $arc115 ]]; then
                        recherche120
                    fi
                fi
            fi
        fi
    fi
}

recherche110() {
    if [[ $ran_a -ne $nombre106 || $arc_actuel -ne $arc106 ]]; then
        if [[ $ran_a -ne $nombre107A || $arc_actuel -ne $arc107 ]]; then
            if [[ $ran_a -ne $nombre108A || $arc_actuel -ne $arc108 ]]; then
                if [[ $ran_a -ne $nombre109 || $arc_actuel -ne $arc109 ]]; then
                    if [[ $ran_a -ne $nombre110 || $arc_actuel -ne $arc110 ]]; then
                        recherche115
                    fi
                fi
            fi
        fi
    fi
}

recherche105() {
    if [[ $ran_a -ne $nombre101 || $arc_actuel -ne $arc101 ]]; then
        if [[ $ran_a -ne $nombre102 || $arc_actuel -ne $arc102 ]]; then
            if [[ $ran_a -ne $nombre103 || $arc_actuel -ne $arc103 ]]; then
                if [[ $ran_a -ne $nombre104 || $arc_actuel -ne $arc104 ]]; then
                    if [[ $ran_a -ne $nombre105 || $arc_actuel -ne $arc105 ]]; then
                        recherche110
                    fi
                fi
            fi
        fi
    fi
}

recherche100() {
    if [[ $ran_a -ne $nombre96 || $arc_actuel -ne $arc96 ]]; then
        if [[ $ran_a -ne $nombre97 || $arc_actuel -ne $arc97 ]]; then
            if [[ $ran_a -ne $nombre98 || $arc_actuel -ne $arc98 ]]; then
                if [[ $ran_a -ne $nombre99 || $arc_actuel -ne $arc99 ]]; then
                    if [[ $ran_a -ne $nombre100 || $arc_actuel -ne $arc100 ]]; then
                        recherche105
                    fi
                fi
            fi
        fi
    fi
}

recherche95() {
    if [[ $ran_a -ne $nombre91 || $arc_actuel -ne $arc91 ]]; then
        if [[ $ran_a -ne $nombre92 || $arc_actuel -ne $arc92 ]]; then
            if [[ $ran_a -ne $nombre93 || $arc_actuel -ne $arc93 ]]; then
                if [[ $ran_a -ne $nombre94 || $arc_actuel -ne $arc94 ]]; then
                    if [[ $ran_a -ne $nombre95 || $arc_actuel -ne $arc95 ]]; then
                        recherche100
                    fi
                fi
            fi
        fi
    fi
}

recherche90() {
    if [[ $ran_a -ne $nombre86 || $arc_actuel -ne $arc86 ]]; then
        if [[ $ran_a -ne $nombre87 || $arc_actuel -ne $arc87 ]]; then
            if [[ $ran_a -ne $nombre88 || $arc_actuel -ne $arc88 ]]; then
                if [[ $ran_a -ne $nombre89 || $arc_actuel -ne $arc89 ]]; then
                    if [[ $ran_a -ne $nombre90 || $arc_actuel -ne $arc90 ]]; then
                        recherche95
                    fi
                fi
            fi
        fi
    fi
}

recherche85() {
    if [[ $ran_a -ne $nombre81 || $arc_actuel -ne $arc81 ]]; then
        if [[ $ran_a -ne $nombre82 || $arc_actuel -ne $arc82 ]]; then
            if [[ $ran_a -ne $nombre83 || $arc_actuel -ne $arc83 ]]; then
                if [[ $ran_a -ne $nombre84 || $arc_actuel -ne $arc84 ]]; then
                    if [[ $ran_a -ne $nombre85 || $arc_actuel -ne $arc85 ]]; then
                        recherche90
                    fi
                fi
            fi
        fi
    fi
}

recherche80() {
    if [[ $ran_a -ne $nombre76 || $arc_actuel -ne $arc76 ]]; then
        if [[ $ran_a -ne $nombre77 || $arc_actuel -ne $arc77 ]]; then
            if [[ $ran_a -ne $nombre78 || $arc_actuel -ne $arc78 ]]; then
                if [[ $ran_a -ne $nombre79 || $arc_actuel -ne $arc79 ]]; then
                    if [[ $ran_a -ne $nombre80 || $arc_actuel -ne $arc80 ]]; then
                        recherche85
                    fi
                fi
            fi
        fi
    fi
}

recherche75() {
    if [[ $ran_a -ne $nombre71 || $arc_actuel -ne $arc71 ]]; then
        if [[ $ran_a -ne $nombre72 || $arc_actuel -ne $arc72 ]]; then
            if [[ $ran_a -ne $nombre73 || $arc_actuel -ne $arc73 ]]; then
                if [[ $ran_a -ne $nombre74 || $arc_actuel -ne $arc74 ]]; then
                    if [[ $ran_a -ne $nombre75 || $arc_actuel -ne $arc75 ]]; then
                        recherche80
                    fi
                fi
            fi
        fi
    fi
}

recherche70() {
    if [[ $ran_a -ne $nombre66 || $arc_actuel -ne $arc66 ]]; then
        if [[ $ran_a -ne $nombre67 || $arc_actuel -ne $arc67 ]]; then
            if [[ $ran_a -ne $nombre68 || $arc_actuel -ne $arc68 ]]; then
                if [[ $ran_a -ne $nombre69 || $arc_actuel -ne $arc69 ]]; then
                    if [[ $ran_a -ne $nombre70 || $arc_actuel -ne $arc70 ]]; then
                        recherche75
                    fi
                fi
            fi
        fi
    fi
}

recherche65() {
    if [[ $ran_a -ne $nombre61 || $arc_actuel -ne $arc61 ]]; then
        if [[ $ran_a -ne $nombre62 || $arc_actuel -ne $arc62 ]]; then
            if [[ $ran_a -ne $nombre63 || $arc_actuel -ne $arc63 ]]; then
                if [[ $ran_a -ne $nombre64 || $arc_actuel -ne $arc64 ]]; then
                    if [[ $ran_a -ne $nombre65 || $arc_actuel -ne $arc65 ]]; then
                        recherche70
                    fi
                fi
            fi
        fi
    fi
}

recherche60() {
    if [[ $ran_a -ne $nombre56 || $arc_actuel -ne $arc56 ]]; then
        if [[ $ran_a -ne $nombre57 || $arc_actuel -ne $arc57 ]]; then
            if [[ $ran_a -ne $nombre58 || $arc_actuel -ne $arc58 ]]; then
                if [[ $ran_a -ne $nombre59 || $arc_actuel -ne $arc59 ]]; then
                    if [[ $ran_a -ne $nombre60 || $arc_actuel -ne $arc60 ]]; then
                        recherche65
                    fi
                fi
            fi
        fi
    fi
}

recherche55() {
    if [[ $ran_a -ne $nombre51 || $arc_actuel -ne $arc51 ]]; then
        if [[ $ran_a -ne $nombre52 || $arc_actuel -ne $arc52 ]]; then
            if [[ $ran_a -ne $nombre53 || $arc_actuel -ne $arc53 ]]; then
                if [[ $ran_a -ne $nombre54 || $arc_actuel -ne $arc54 ]]; then
                    if [[ $ran_a -ne $nombre55 || $arc_actuel -ne $arc55 ]]; then
                        recherche60
                    fi
                fi
            fi
        fi
    fi
}

recherche50() {
    if [[ $ran_a -ne $nombre46 || $arc_actuel -ne $arc46 ]]; then
        if [[ $ran_a -ne $nombre47 || $arc_actuel -ne $arc47 ]]; then
            if [[ $ran_a -ne $nombre48 || $arc_actuel -ne $arc48 ]]; then
                if [[ $ran_a -ne $nombre49 || $arc_actuel -ne $arc49 ]]; then
                    if [[ $ran_a -ne $nombre50 || $arc_actuel -ne $arc50 ]]; then
                        recherche55
                    fi
                fi
            fi
        fi
    fi
}

recherche45() {
    if [[ $ran_a -ne $nombre41 || $arc_actuel -ne $arc41 ]]; then
        if [[ $ran_a -ne $nombre42 || $arc_actuel -ne $arc42 ]]; then
            if [[ $ran_a -ne $nombre43 || $arc_actuel -ne $arc43 ]]; then
                if [[ $ran_a -ne $nombre44 || $arc_actuel -ne $arc44 ]]; then
                    if [[ $ran_a -ne $nombre45 || $arc_actuel -ne $arc45 ]]; then
                        recherche50
                    fi
                fi
            fi
        fi
    fi
}

recherche40() {
    if [[ $ran_a -ne $nombre36 || $arc_actuel -ne $arc36 ]]; then
        if [[ $ran_a -ne $nombre37 || $arc_actuel -ne $arc37 ]]; then
            if [[ $ran_a -ne $nombre38 || $arc_actuel -ne $arc38 ]]; then
                if [[ $ran_a -ne $nombre39 || $arc_actuel -ne $arc39 ]]; then
                    if [[ $ran_a -ne $nombre40 || $arc_actuel -ne $arc40 ]]; then
                        recherche45
                    fi
                fi
            fi
        fi
    fi
}

recherche35() {
    if [[ $ran_a -ne $nombre31 || $arc_actuel -ne $arc31 ]]; then
        if [[ $ran_a -ne $nombre32 || $arc_actuel -ne $arc32 ]]; then
            if [[ $ran_a -ne $nombre33 || $arc_actuel -ne $arc33 ]]; then
                if [[ $ran_a -ne $nombre34 || $arc_actuel -ne $arc34 ]]; then
                    if [[ $ran_a -ne $nombre35 || $arc_actuel -ne $arc35 ]]; then
                        recherche40
                    fi
                fi
            fi
        fi
    fi
}

recherche30() {
    if [[ $ran_a -ne $nombre26 || $arc_actuel -ne $arc26 ]]; then
        if [[ $ran_a -ne $nombre27 || $arc_actuel -ne $arc27 ]]; then
            if [[ $ran_a -ne $nombre28 || $arc_actuel -ne $arc28 ]]; then
                if [[ $ran_a -ne $nombre29 || $arc_actuel -ne $arc29 ]]; then
                    if [[ $ran_a -ne $nombre30 || $arc_actuel -ne $arc30 ]]; then
                        recherche35
                    fi
                fi
            fi
        fi
    fi
}

recherche25() {
    if [[ $ran_a -ne $nombre21 || $arc_actuel -ne $arc21 ]]; then
        if [[ $ran_a -ne $nombre22 || $arc_actuel -ne $arc22 ]]; then
            if [[ $ran_a -ne $nombre23 || $arc_actuel -ne $arc23 ]]; then
                if [[ $ran_a -ne $nombre24 || $arc_actuel -ne $arc24 ]]; then
                    if [[ $ran_a -ne $nombre25 || $arc_actuel -ne $arc25 ]]; then
                        recherche30
                    fi
                fi
            fi
        fi
    fi
}

recherche20() {
    if [[ $ran_a -ne $nombre16 || $arc_actuel -ne $arc16 ]]; then
        if [[ $ran_a -ne $nombre17 || $arc_actuel -ne $arc17 ]]; then
            if [[ $ran_a -ne $nombre18 || $arc_actuel -ne $arc18 ]]; then
                if [[ $ran_a -ne $nombre19 || $arc_actuel -ne $arc19 ]]; then
                    if [[ $ran_a -ne $nombre20 || $arc_actuel -ne $arc20 ]]; then
                        recherche25
                    fi
                fi
            fi
        fi
    fi
}

recherche15() {
    if [[ $ran_a -ne $nombre11 || $arc_actuel -ne $arc11 ]]; then
        if [[ $ran_a -ne $nombre12 || $arc_actuel -ne $arc12 ]]; then
            if [[ $ran_a -ne $nombre13 || $arc_actuel -ne $arc13 ]]; then
                if [[ $ran_a -ne $nombre14 || $arc_actuel -ne $arc14 ]]; then
                    if [[ $ran_a -ne $nombre15 || $arc_actuel -ne $arc15 ]]; then
                        recherche20
                    fi
                fi
            fi
        fi
    fi
}

recherche10() {
    if [[ $ran_a -ne $nombre6 || $arc_actuel -ne $arc6 ]]; then
        if [[ $ran_a -ne $nombre7 || $arc_actuel -ne $arc7 ]]; then
            if [[ $ran_a -ne $nombre8 || $arc_actuel -ne $arc8 ]]; then
                if [[ $ran_a -ne $nombre9 || $arc_actuel -ne $arc9 ]]; then
                    if [[ $ran_a -ne $nombre10 || $arc_actuel -ne $arc10 ]]; then
                        recherche15
                    fi
                fi
            fi
        fi
    fi
}

recherche5() {
    if [[ $ran_a -ne $nombre1 || $arc_actuel -ne $arc1 ]]; then
        if [[ $ran_a -ne $nombre2 || $arc_actuel -ne $arc2 ]]; then
            if [[ $ran_a -ne $nombre3 || $arc_actuel -ne $arc3 ]]; then
                if [[ $ran_a -ne $nombre4 || $arc_actuel -ne $arc4 ]]; then
                    if [[ $ran_a -ne $nombre5 || $arc_actuel -ne $arc5 ]]; then
                        recherche10
                    fi
                fi
            fi
        fi
    fi
}

recherche5
