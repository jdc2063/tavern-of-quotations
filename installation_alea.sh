installation300() {
    if [[ $nombre296 -eq 0 && $arc296 -ne $arc_actuel ]]; then
        nombre296=$ran_a
        arc296=$arc_actuel
        personnage296=$raiponce
        
    else
        if [[ $nombre297 -eq 0 && $arc297 -ne $arc_actuel ]]; then
            nombre297=$ran_a
            arc297=$arc_actuel
            personnage297=$raiponce
            
        else
            if [[ $nombre298 -eq 0 && $arc298 -ne $arc_actuel ]]; then
                nombre298=$ran_a
                arc298=$arc_actuel
                personnage298=$raiponce
                
            else
                if [[ $nombre299 -eq 0 && $arc299 -ne $arc_actuel ]]; then
                    nombre299=$ran_a
                    arc299=$arc_actuel
                    personnage299=$raiponce
                    
                else
                    if [[ $nombre300 -eq 0 && $arc300 -ne $arc_actuel ]]; then
                        nombre300=$ran_a
                        arc300=$arc_actuel
                        personnage300=$raiponce
                        
                    else
                        echo "plus de place"
                    fi
                fi
            fi
        fi
    fi
}

installation295() {
    if [[ $nombre291 -eq 0 && $arc291 -ne $arc_actuel ]]; then
        nombre291=$ran_a
        arc291=$arc_actuel
        personnage291=$raiponce
        
    else
        if [[ $nombre292 -eq 0 && $arc292 -ne $arc_actuel ]]; then
            nombre292=$ran_a
            arc292=$arc_actuel
            personnage292=$raiponce
            
        else
            if [[ $nombre293 -eq 0 && $arc293 -ne $arc_actuel ]]; then
                nombre293=$ran_a
                arc293=$arc_actuel
                personnage293=$raiponce
                
            else
                if [[ $nombre294 -eq 0 && $arc294 -ne $arc_actuel ]]; then
                    nombre294=$ran_a
                    arc294=$arc_actuel
                    personnage294=$raiponce
                    
                else
                    if [[ $nombre295 -eq 0 && $arc295 -ne $arc_actuel ]]; then
                        nombre295=$ran_a
                        arc295=$arc_actuel
                        personnage295=$raiponce
                        
                    else
                        installation300
                    fi
                fi
            fi
        fi
    fi
}

installation290() {
    if [[ $nombre286 -eq 0 && $arc286 -ne $arc_actuel ]]; then
        nombre286=$ran_a
        arc286=$arc_actuel
        personnage286=$raiponce
        
    else
        if [[ $nombre287 -eq 0 && $arc287 -ne $arc_actuel ]]; then
            nombre287=$ran_a
            arc287=$arc_actuel
            personnage287=$raiponce
            
        else
            if [[ $nombre288 -eq 0 && $arc288 -ne $arc_actuel ]]; then
                nombre288=$ran_a
                arc288=$arc_actuel
                personnage288=$raiponce
                
            else
                if [[ $nombre289 -eq 0 && $arc289 -ne $arc_actuel ]]; then
                    nombre289=$ran_a
                    arc289=$arc_actuel
                    personnage289=$raiponce
                    
                else
                    if [[ $nombre290 -eq 0 && $arc290 -ne $arc_actuel ]]; then
                        nombre290=$ran_a
                        arc290=$arc_actuel
                        personnage290=$raiponce
                        
                    else
                        installation295
                    fi
                fi
            fi
        fi
    fi
}

installation285() {
    if [[ $nombre281 -eq 0 && $arc281 -ne $arc_actuel ]]; then
        nombre281=$ran_a
        arc281=$arc_actuel
        personnage281=$raiponce
        
    else
        if [[ $nombre282 -eq 0 && $arc282 -ne $arc_actuel ]]; then
            nombre282=$ran_a
            arc282=$arc_actuel
            personnage282=$raiponce
            
        else
            if [[ $nombre283 -eq 0 && $arc283 -ne $arc_actuel ]]; then
                nombre283=$ran_a
                arc283=$arc_actuel
                personnage283=$raiponce
                
            else
                if [[ $nombre284 -eq 0 && $arc284 -ne $arc_actuel ]]; then
                    nombre284=$ran_a
                    arc284=$arc_actuel
                    personnage284=$raiponce
                    
                else
                    if [[ $nombre285 -eq 0 && $arc285 -ne $arc_actuel ]]; then
                        nombre285=$ran_a
                        arc285=$arc_actuel
                        personnage285=$raiponce
                        
                    else
                        installation290
                    fi
                fi
            fi
        fi
    fi
}

installation280() {
    if [[ $nombre276 -eq 0 && $arc276 -ne $arc_actuel ]]; then
        nombre276=$ran_a
        arc276=$arc_actuel
        personnage276=$raiponce
        
    else
        if [[ $nombre277 -eq 0 && $arc277 -ne $arc_actuel ]]; then
            nombre277=$ran_a
            arc277=$arc_actuel
            personnage277=$raiponce
            
        else
            if [[ $nombre278 -eq 0 && $arc278 -ne $arc_actuel  ]]; then
                nombre278=$ran_a
                arc278=$arc_actuel
                personnage278=$raiponce
                
            else
                if [[ $nombre279 -eq 0 && $arc279 -ne $arc_actuel ]]; then
                    nombre279=$ran_a
                    arc279=$arc_actuel
                    personnage279=$raiponce
                    
                else
                    if [[ $nombre280 -eq 0 && $arc280 -ne $arc_actuel ]]; then
                        nombre280=$ran_a
                        arc280=$arc_actuel
                        personnage280=$raiponce
                        
                    else
                        installation285
                    fi
                fi
            fi
        fi
    fi
}

installation275() {
    if [[ $nombre271 -eq 0 && $arc271 -ne $arc_actuel ]]; then
        nombre271=$ran_a
        arc271=$arc_actuel
        personnage271=$raiponce
        
    else
        if [[ $nombre272 -eq 0 && $arc272 -ne $arc_actuel ]]; then
            nombre272=$ran_a
            arc272=$arc_actuel
            personnage272=$raiponce
            
        else
            if [[ $nombre273 -eq 0 && $arc273 -ne $arc_actuel ]]; then
                nombre273=$ran_a
                arc273=$arc_actuel
                personnage273=$raiponce
                
            else
                if [[ $nombre274 -eq 0 && $arc274 -ne $arc_actuel ]]; then
                    nombre274=$ran_a
                    arc274=$arc_actuel
                    personnage274=$raiponce
                    
                else
                    if [[ $nombre275 -eq 0 && $arc275 -ne $arc_actuel ]]; then
                        nombre275=$ran_a
                        arc275=$arc_actuel
                        personnage275=$raiponce
                        
                    else
                        installation280
                    fi
                fi
            fi
        fi
    fi
}

installation270() {
    if [[ $nombre266 -eq 0 && $arc266 -ne $arc_actuel ]]; then
        nombre266=$ran_a
        arc266=$arc_actuel
        personnage266=$raiponce
        
    else
        if [[ $nombre267 -eq 0 && $arc267 -ne $arc_actuel ]]; then
            nombre267=$ran_a
            arc267=$arc_actuel
            personnage267=$raiponce
            
        else
            if [[ $nombre268 -eq 0 && $arc268 -ne $arc_actuel ]]; then
                nombre268=$ran_a
                arc268=$arc_actuel
                personnage268=$raiponce
                
            else
                if [[ $nombre269 -eq 0 && $arc269 -ne $arc_actuel ]]; then
                    nombre269=$ran_a
                    arc269=$arc_actuel
                    personnage269=$raiponce
                    
                else
                    if [[ $nombre270 -eq 0 && $arc270 -ne $arc_actuel ]]; then
                        nombre270=$ran_a
                        arc270=$arc_actuel
                        personnage270=$raiponce
                        
                    else
                        installation275
                    fi
                fi
            fi
        fi
    fi
}

installation265() {
    if [[ $nombre261 -eq 0 && $arc261 -ne $arc_actuel ]]; then
        nombre261=$ran_a
        arc261=$arc_actuel
        personnage261=$raiponce
        
    else
        if [[ $nombre262 -eq 0 && $arc262 -ne $arc_actuel ]]; then
            nombre262=$ran_a
            arc262=$arc_actuel
            personnage262=$raiponce
            
        else
            if [[ $nombre263 -eq 0 && $arc263 -ne $arc_actuel ]]; then
                nombre263=$ran_a
                arc263=$arc_actuel
                personnage263=$raiponce
                
            else
                if [[ $nombre264 -eq 0 && $arc264 -ne $arc_actuel ]]; then
                    nombre264=$ran_a
                    arc264=$arc_actuel
                    personnage264=$raiponce
                    
                else
                    if [[ $nombre265 -eq 0 && $arc265 -ne $arc_actuel ]]; then
                        nombre265=$ran_a
                        arc265=$arc_actuel
                        personnage265=$raiponce
                        
                    else
                        installation270
                    fi
                fi
            fi
        fi
    fi
}

installation260() {
    if [[ $nombre256 -eq 0 && $arc256 -ne $arc_actuel ]]; then
        nombre256=$ran_a
        arc256=$arc_actuel
        personnage256=$raiponce
        
    else
        if [[ $nombre257 -eq 0 && $arc257 -ne $arc_actuel ]]; then
            nombre257=$ran_a
            arc257=$arc_actuel
            personnage257=$raiponce
            
        else
            if [[ $nombre258 -eq 0 && $arc258 -ne $arc_actuel ]]; then
                nombre258=$ran_a
                arc258=$arc_actuel
                personnage258=$raiponce
                
            else
                if [[ $nombre259 -eq 0 && $arc259 -ne $arc_actuel ]]; then
                    nombre259=$ran_a
                    arc259=$arc_actuel
                    personnage259=$raiponce
                    
                else
                    if [[ $nombre260 -eq 0 && $arc260 -ne $arc_actuel ]]; then
                        nombre260=$ran_a
                        arc260=$arc_actuel
                        personnage260=$raiponce
                        
                    else
                        installation265
                    fi
                fi
            fi
        fi
    fi
}

installation255() {
    if [[ $nombre251 -eq 0 && $arc251 -ne $arc_actuel ]]; then
        nombre251=$ran_a
        arc251=$arc_actuel
        personnage251=$raiponce
        
    else
        if [[ $nombre252 -eq 0 && $arc252 -ne $arc_actuel ]]; then
            nombre252=$ran_a
            arc252=$arc_actuel
            personnage252=$raiponce
            
        else
            if [[ $nombre253 -eq 0 && $arc253 -ne $arc_actuel ]]; then
                nombre253=$ran_a
                arc253=$arc_actuel
                personnage253=$raiponce
                
            else
                if [[ $nombre254 -eq 0 && $arc254 -ne $arc_actuel ]]; then
                    nombre254=$ran_a
                    arc254=$arc_actuel
                    personnage254=$raiponce
                    
                else
                    if [[ $nombre255 -eq 0 && $arc255 -ne $arc_actuel ]]; then
                        nombre255=$ran_a
                        arc255=$arc_actuel
                        personnage255=$raiponce
                        
                    else
                        installation260
                    fi
                fi
            fi
        fi
    fi
}

installation250() {
    if [[ $nombre246 -eq 0 && $arc246 -ne $arc_actuel ]]; then
        nombre246=$ran_a
        arc246=$arc_actuel
        personnage246=$raiponce
        
    else
        if [[ $nombre247 -eq 0 && $arc247 -ne $arc_actuel ]]; then
            nombre247=$ran_a
            arc247=$arc_actuel
            personnage247=$raiponce
            
        else
            if [[ $nombre248 -eq 0 && $arc248 -ne $arc_actuel ]]; then
                nombre248=$ran_a
                arc248=$arc_actuel
                personnage248=$raiponce
                
            else
                if [[ $nombre249 -eq 0 && $arc249 -ne $arc_actuel ]]; then
                    nombre249=$ran_a
                    arc249=$arc_actuel
                    personnage249=$raiponce
                    
                else
                    if [[ $nombre250 -eq 0 && $arc250 -ne $arc_actuel ]]; then
                        nombre250=$ran_a
                        arc250=$arc_actuel
                        personnage250=$raiponce
                        
                    else
                        installation255
                    fi
                fi
            fi
        fi
    fi
}

installation245() {
    if [[ $nombre241 -eq 0 && $arc241 -ne $arc_actuel ]]; then
        nombre241=$ran_a
        arc241=$arc_actuel
        personnage241=$raiponce
        
    else
        if [[ $nombre242 -eq 0 && $arc242 -ne $arc_actuel ]]; then
            nombre242=$ran_a
            arc242=$arc_actuel
            personnage242=$raiponce
            
        else
            if [[ $nombre243 -eq 0 && $arc243 -ne $arc_actuel ]]; then
                nombre243=$ran_a
                arc243=$arc_actuel
                personnage243=$raiponce
                
            else
                if [[ $nombre244 -eq 0 && $arc244 -ne $arc_actuel ]]; then
                    nombre244=$ran_a
                    arc244=$arc_actuel
                    personnage244=$raiponce
                    
                else
                    if [[ $nombre245 -eq 0 && $arc245 -ne $arc_actuel ]]; then
                        nombre245=$ran_a
                        arc245=$arc_actuel
                        personnage245=$raiponce
                        
                    else
                        installation250
                    fi
                fi
            fi
        fi
    fi
}

installation240() {
    if [[ $nombre236 -eq 0 && $arc236 -ne $arc_actuel ]]; then
        nombre236=$ran_a
        arc236=$arc_actuel
        personnage236=$raiponce
        
    else
        if [[ $nombre237 -eq 0 && $arc237 -ne $arc_actuel ]]; then
            nombre237=$ran_a
            arc237=$arc_actuel
            personnage237=$raiponce
            
        else
            if [[ $nombre238 -eq 0 && $arc238 -ne $arc_actuel ]]; then
                nombre238=$ran_a
                arc238=$arc_actuel
                personnage238=$raiponce
                
            else
                if [[ $nombre239 -eq 0 && $arc239 -ne $arc_actuel ]]; then
                    nombre239=$ran_a
                    arc239=$arc_actuel
                    personnage239=$raiponce
                    
                else
                    if [[ $nombre240 -eq 0 && $arc240 -ne $arc_actuel ]]; then
                        nombre240=$ran_a
                        arc240=$arc_actuel
                        personnage240=$raiponce
                        
                    else
                        installation245
                    fi
                fi
            fi
        fi
    fi
}

installation235() {
    if [[ $nombre231 -eq 0 && $arc231 -ne $arc_actuel ]]; then
        nombre231=$ran_a
        arc231=$arc_actuel
        personnage231=$raiponce
        
    else
        if [[ $nombre232 -eq 0 && $arc232 -ne $arc_actuel ]]; then
            nombre232=$ran_a
            arc232=$arc_actuel
            personnage232=$raiponce
            
        else
            if [[ $nombre233 -eq 0 && $arc233 -ne $arc_actuel ]]; then
                nombre233=$ran_a
                arc233=$arc_actuel
                personnage233=$raiponce
                
            else
                if [[ $nombre234 -eq 0 && $arc234 -ne $arc_actuel ]]; then
                    nombre234=$ran_a
                    arc234=$arc_actuel
                    personnage234=$raiponce
                    
                else
                    if [[ $nombre235 -eq 0 && $arc235 -ne $arc_actuel ]]; then
                        nombre235=$ran_a
                        arc235=$arc_actuel
                        personnage235=$raiponce
                        
                    else
                        installation240
                    fi
                fi
            fi
        fi
    fi
}

installation230() {
    if [[ $nombre226 -eq 0 && $arc226 -ne $arc_actuel ]]; then
        nombre226=$ran_a
        arc226=$arc_actuel
        personnage226=$raiponce
        
    else
        if [[ $nombre227 -eq 0 && $arc227 -ne $arc_actuel ]]; then
            nombre227=$ran_a
            arc227=$arc_actuel
            personnage227=$raiponce
            
        else
            if [[ $nombre228 -eq 0 && $arc228 -ne $arc_actuel ]]; then
                nombre228=$ran_a
                arc228=$arc_actuel
                personnage228=$raiponce
                
            else
                if [[ $nombre229 -eq 0 && $arc229 -ne $arc_actuel ]]; then
                    nombre229=$ran_a
                    arc229=$arc_actuel
                    personnage229=$raiponce
                    
                else
                    if [[ $nombre230 -eq 0 && $arc230 -ne $arc_actuel ]]; then
                        nombre230=$ran_a
                        arc230=$arc_actuel
                        personnage230=$raiponce
                        
                    else
                        installation235
                    fi
                fi
            fi
        fi
    fi
}

installation225() {
    if [[ $nombre221 -eq 0 && $arc221 -ne $arc_actuel ]]; then
        nombre221=$ran_a
        arc221=$arc_actuel
        personnage221=$raiponce
        
    else
        if [[ $nombre222 -eq 0 && $arc222 -ne $arc_actuel ]]; then
            nombre222=$ran_a
            arc222=$arc_actuel
            personnage222=$raiponce
            
        else
            if [[ $nombre223 -eq 0 && $arc223 -ne $arc_actuel ]]; then
                nombre223=$ran_a
                arc223=$arc_actuel
                personnage223=$raiponce
                
            else
                if [[ $nombre224 -eq 0 && $arc224 -ne $arc_actuel ]]; then
                    nombre224=$ran_a
                    arc224=$arc_actuel
                    personnage224=$raiponce
                    
                else
                    if [[ $nombre225 -eq 0 && $arc225 -ne $arc_actuel ]]; then
                        nombre225=$ran_a
                        arc225=$arc_actuel
                        personnage225=$raiponce
                        
                    else
                        installation230
                    fi
                fi
            fi
        fi
    fi
}

installation220() {
    if [[ $nombre216 -eq 0 && $arc216 -ne $arc_actuel ]]; then
        nombre216=$ran_a
        arc216=$arc_actuel
        personnage216=$raiponce
        
    else
        if [[ $nombre217 -eq 0 && $arc217 -ne $arc_actuel ]]; then
            nombre217=$ran_a
            arc217=$arc_actuel
            personnage217=$raiponce
            
        else
            if [[ $nombre218 -eq 0 && $arc218 -ne $arc_actuel ]]; then
                nombre218=$ran_a
                arc218=$arc_actuel
                personnage218=$raiponce
                
            else
                if [[ $nombre219 -eq 0 && $arc219 -ne $arc_actuel ]]; then
                    nombre219=$ran_a
                    arc219=$arc_actuel
                    personnage219=$raiponce
                    
                else
                    if [[ $nombre220 -eq 0 && $arc220 -ne $arc_actuel ]]; then
                        nombre220=$ran_a
                        arc220=$arc_actuel
                        personnage220=$raiponce
                        
                    else
                        installation225
                    fi
                fi
            fi
        fi
    fi
} 

installation215() {
    if [[ $nombre211 -eq 0 && $arc211 -ne $arc_actuel ]]; then
        nombre211=$ran_a
        arc211=$arc_actuel
        personnage211=$raiponce
        
    else
        if [[ $nombre212 -eq 0 && $arc212 -ne $arc_actuel ]]; then
            nombre212=$ran_a
            arc212=$arc_actuel
            personnage212=$raiponce
            
        else
            if [[ $nombre213 -eq 0 && $arc213 -ne $arc_actuel ]]; then
                nombre213=$ran_a
                arc213=$arc_actuel
                personnage213=$raiponce
                
            else
                if [[ $nombre214 -eq 0 && $arc214 -ne $arc_actuel ]]; then
                    nombre214=$ran_a
                    arc214=$arc_actuel
                    personnage214=$raiponce
                    
                else
                    if [[ $nombre215 -eq 0 && $arc215 -ne $arc_actuel ]]; then
                        nombre215=$ran_a
                        arc215=$arc_actuel
                        personnage215=$raiponce
                        
                    else
                        installation220
                    fi
                fi
            fi
        fi
    fi
}

installation210() {
    if [[ $nombre206 -eq 0 && $arc206 -ne $arc_actuel ]]; then
        nombre206=$ran_a
        arc206=$arc_actuel
        personnage206=$raiponce
        
    else
        if [[ $nombre207 -eq 0 && $arc207 -ne $arc_actuel ]]; then
            nombre207=$ran_a
            arc207=$arc_actuel
            personnage207=$raiponce
            
        else
            if [[ $nombre208 -eq 0 && $arc208 -ne $arc_actuel ]]; then
                nombre208=$ran_a
                arc208=$arc_actuel
                personnage208=$raiponce
                
            else
                if [[ $nombre209 -eq 0 && $arc209 -ne $arc_actuel ]]; then
                    nombre209=$ran_a
                    arc209=$arc_actuel
                    personnage209=$raiponce
                    
                else
                    if [[ $nombre210 -eq 0 && $arc210 -ne $arc_actuel ]]; then
                        nombre210=$ran_a
                        arc210=$arc_actuel
                        personnage210=$raiponce
                        
                    else
                        installation215
                    fi
                fi
            fi
        fi
    fi
}

installation205() {
    if [[ $nombre201 -eq 0 && $arc201 -ne $arc_actuel ]]; then
        nombre201=$ran_a
        arc201=$arc_actuel
        personnage201=$raiponce
        
    else
        if [[ $nombre202 -eq 0 && $arc202 -ne $arc_actuel ]]; then
            nombre202=$ran_a
            arc202=$arc_actuel
            personnage202=$raiponce
            
        else
            if [[ $nombre203 -eq 0 && $arc203 -ne $arc_actuel ]]; then
                nombre203=$ran_a
                arc203=$arc_actuel
                personnage203=$raiponce
                
            else
                if [[ $nombre204 -eq 0 && $arc204 -ne $arc_actuel ]]; then
                    nombre204=$ran_a
                    arc204=$arc_actuel
                    personnage204=$raiponce
                    
                else
                    if [[ $nombre205 -eq 0 && $arc205 -ne $arc_actuel ]]; then
                        nombre205=$ran_a
                        arc205=$arc_actuel
                        personnage205=$raiponce
                        
                    else
                        installation210
                    fi
                fi
            fi
        fi
    fi
}

installation200() {
    if [[ $nombre196 -eq 0 && $arc196 -ne $arc_actuel ]]; then
        nombre196=$ran_a
        arc196=$arc_actuel
        personnage196=$raiponce
        
    else
        if [[ $nombre197 -eq 0 && $arc197 -ne $arc_actuel ]]; then
            nombre197=$ran_a
            arc197=$arc_actuel
            personnage197=$raiponce
            
        else
            if [[ $nombre198 -eq 0 && $arc198 -ne $arc_actuel ]]; then
                nombre198=$ran_a
                arc198=$arc_actuel
                personnage198=$raiponce
                
            else
                if [[ $nombre199 -eq 0 && $arc199 -ne $arc_actuel ]]; then
                    nombre199=$ran_a
                    arc199=$arc_actuel
                    personnage199=$raiponce
                    
                else
                    if [[ $nombre200 -eq 0 && $arc200 -ne $arc_actuel ]]; then
                        nombre200=$ran_a
                        arc200=$arc_actuel
                        personnage200=$raiponce
                        
                    else
                        installation205
                    fi
                fi
            fi
        fi
    fi
}

installation195() {
    if [[ $nombre191 -eq 0 && $arc191 -ne $arc_actuel ]]; then
        nombre191=$ran_a
        arc191=$arc_actuel
        personnage191=$raiponce
        
    else
        if [[ $nombre192 -eq 0 && $arc192 -ne $arc_actuel ]]; then
            nombre192=$ran_a
            arc192=$arc_actuel
            personnage192=$raiponce
            
        else
            if [[ $nombre193 -eq 0 && $arc193 -ne $arc_actuel ]]; then
                nombre193=$ran_a
                arc193=$arc_actuel
                personnage193=$raiponce
                
            else
                if [[ $nombre194 -eq 0 && $arc194 -ne $arc_actuel ]]; then
                    nombre194=$ran_a
                    arc194=$arc_actuel
                    personnage194=$raiponce
                    
                else
                    if [[ $nombre195 -eq 0 && $arc195 -ne $arc_actuel ]]; then
                        nombre195=$ran_a
                        arc195=$arc_actuel
                        personnage195=$raiponce
                        
                    else
                        installation200
                    fi
                fi
            fi
        fi
    fi
}

installation190() {
    if [[ $nombre186 -eq 0 && $arc186 -ne $arc_actuel ]]; then
        nombre186=$ran_a
        arc186=$arc_actuel
        personnage186=$raiponce
        
    else
        if [[ $nombre187 -eq 0 && $arc187 -ne $arc_actuel ]]; then
            nombre187=$ran_a
            arc187=$arc_actuel
            personnage187=$raiponce
            
        else
            if [[ $nombre188 -eq 0 && $arc188 -ne $arc_actuel ]]; then
                nombre188=$ran_a
                arc188=$arc_actuel
                personnage188=$raiponce
                
            else
                if [[ $nombre189 -eq 0 && $arc189 -ne $arc_actuel ]]; then
                    nombre189=$ran_a
                    arc189=$arc_actuel
                    personnage189=$raiponce
                    
                else
                    if [[ $nombre190 -eq 0 && $arc190 -ne $arc_actuel ]]; then
                        nombre190=$ran_a
                        arc190=$arc_actuel
                        personnage190=$raiponce
                        
                    else
                        installation195
                    fi
                fi
            fi
        fi
    fi
}

installation185() {
    if [[ $nombre181 -eq 0 && $arc181 -ne $arc_actuel ]]; then
        nombre181=$ran_a
        arc181=$arc_actuel
        personnage181=$raiponce
        
    else
        if [[ $nombre182 -eq 0 && $arc182 -ne $arc_actuel ]]; then
            nombre182=$ran_a
            arc182=$arc_actuel
            personnage182=$raiponce
            
        else
            if [[ $nombre183 -eq 0 && $arc183 -ne $arc_actuel ]]; then
                nombre183=$ran_a
                arc183=$arc_actuel
                personnage183=$raiponce
                
            else
                if [[ $nombre184 -eq 0 && $arc184 -ne $arc_actuel ]]; then
                    nombre184=$ran_a
                    arc184=$arc_actuel
                    personnage184=$raiponce
                    
                else
                    if [[ $nombre185 -eq 0 && $arc185 -ne $arc_actuel ]]; then
                        nombre185=$ran_a
                        arc185=$arc_actuel
                        personnage185=$raiponce
                        
                    else
                        installation190
                    fi
                fi
            fi
        fi
    fi
}

installation180() {
    if [[ $nombre176 -eq 0 && $arc176 -ne $arc_actuel ]]; then
        nombre176=$ran_a
        arc176=$arc_actuel
        personnage176=$raiponce
        
    else
        if [[ $nombre177 -eq 0 && $arc177 -ne $arc_actuel ]]; then
            nombre177=$ran_a
            arc177=$arc_actuel
            personnage177=$raiponce
            
        else
            if [[ $nombre178 -eq 0 && $arc178 -ne $arc_actuel  ]]; then
                nombre178=$ran_a
                arc178=$arc_actuel
                personnage178=$raiponce
                
            else
                if [[ $nombre179 -eq 0 && $arc179 -ne $arc_actuel ]]; then
                    nombre179=$ran_a
                    arc179=$arc_actuel
                    personnage179=$raiponce
                    
                else
                    if [[ $nombre180 -eq 0 && $arc180 -ne $arc_actuel ]]; then
                        nombre180=$ran_a
                        arc180=$arc_actuel
                        personnage180=$raiponce
                        
                    else
                        installation185
                    fi
                fi
            fi
        fi
    fi
}

installation175() {
    if [[ $nombre171 -eq 0 && $arc171 -ne $arc_actuel ]]; then
        nombre171=$ran_a
        arc171=$arc_actuel
        personnage171=$raiponce
        
    else
        if [[ $nombre172 -eq 0 && $arc172 -ne $arc_actuel ]]; then
            nombre172=$ran_a
            arc172=$arc_actuel
            personnage172=$raiponce
            
        else
            if [[ $nombre173 -eq 0 && $arc173 -ne $arc_actuel ]]; then
                nombre173=$ran_a
                arc173=$arc_actuel
                personnage173=$raiponce
                
            else
                if [[ $nombre174 -eq 0 && $arc174 -ne $arc_actuel ]]; then
                    nombre174=$ran_a
                    arc174=$arc_actuel
                    personnage174=$raiponce
                    
                else
                    if [[ $nombre175 -eq 0 && $arc175 -ne $arc_actuel ]]; then
                        nombre175=$ran_a
                        arc175=$arc_actuel
                        personnage175=$raiponce
                        
                    else
                        installation180
                    fi
                fi
            fi
        fi
    fi
}

installation170() {
    if [[ $nombre166 -eq 0 && $arc166 -ne $arc_actuel ]]; then
        nombre166=$ran_a
        arc166=$arc_actuel
        personnage166=$raiponce
        
    else
        if [[ $nombre167 -eq 0 && $arc167 -ne $arc_actuel ]]; then
            nombre167=$ran_a
            arc167=$arc_actuel
            personnage167=$raiponce
            
        else
            if [[ $nombre168 -eq 0 && $arc168 -ne $arc_actuel ]]; then
                nombre168=$ran_a
                arc168=$arc_actuel
                personnage168=$raiponce
                
            else
                if [[ $nombre169 -eq 0 && $arc169 -ne $arc_actuel ]]; then
                    nombre169=$ran_a
                    arc169=$arc_actuel
                    personnage169=$raiponce
                    
                else
                    if [[ $nombre170 -eq 0 && $arc170 -ne $arc_actuel ]]; then
                        nombre170=$ran_a
                        arc170=$arc_actuel
                        personnage170=$raiponce
                        
                    else
                        installation175
                    fi
                fi
            fi
        fi
    fi
}

installation165() {
    if [[ $nombre161 -eq 0 && $arc161 -ne $arc_actuel ]]; then
        nombre161=$ran_a
        arc161=$arc_actuel
        personnage161=$raiponce
        
    else
        if [[ $nombre162 -eq 0 && $arc162 -ne $arc_actuel ]]; then
            nombre162=$ran_a
            arc162=$arc_actuel
            personnage162=$raiponce
            
        else
            if [[ $nombre163 -eq 0 && $arc163 -ne $arc_actuel ]]; then
                nombre163=$ran_a
                arc163=$arc_actuel
                personnage163=$raiponce
                
            else
                if [[ $nombre164 -eq 0 && $arc164 -ne $arc_actuel ]]; then
                    nombre164=$ran_a
                    arc164=$arc_actuel
                    personnage164=$raiponce
                    
                else
                    if [[ $nombre165 -eq 0 && $arc165 -ne $arc_actuel ]]; then
                        nombre165=$ran_a
                        arc165=$arc_actuel
                        personnage165=$raiponce
                        
                    else
                        installation170
                    fi
                fi
            fi
        fi
    fi
}

installation160() {
    if [[ $nombre156 -eq 0 && $arc156 -ne $arc_actuel ]]; then
        nombre156=$ran_a
        arc156=$arc_actuel
        personnage156=$raiponce
        
    else
        if [[ $nombre157 -eq 0 && $arc157 -ne $arc_actuel ]]; then
            nombre157=$ran_a
            arc157=$arc_actuel
            personnage157=$raiponce
            
        else
            if [[ $nombre158 -eq 0 && $arc158 -ne $arc_actuel ]]; then
                nombre158=$ran_a
                arc158=$arc_actuel
                personnage158=$raiponce
                
            else
                if [[ $nombre159 -eq 0 && $arc159 -ne $arc_actuel ]]; then
                    nombre159=$ran_a
                    arc159=$arc_actuel
                    personnage159=$raiponce
                    
                else
                    if [[ $nombre160 -eq 0 && $arc160 -ne $arc_actuel ]]; then
                        nombre160=$ran_a
                        arc160=$arc_actuel
                        personnage160=$raiponce
                        
                    else
                        installation165
                    fi
                fi
            fi
        fi
    fi
}

installation155() {
    if [[ $nombre151 -eq 0 && $arc151 -ne $arc_actuel ]]; then
        nombre151=$ran_a
        arc151=$arc_actuel
        personnage151=$raiponce
        
    else
        if [[ $nombre152 -eq 0 && $arc152 -ne $arc_actuel ]]; then
            nombre152=$ran_a
            arc152=$arc_actuel
            personnage152=$raiponce
            
        else
            if [[ $nombre153 -eq 0 && $arc153 -ne $arc_actuel ]]; then
                nombre153=$ran_a
                arc153=$arc_actuel
                personnage153=$raiponce
                
            else
                if [[ $nombre154 -eq 0 && $arc154 -ne $arc_actuel ]]; then
                    nombre154=$ran_a
                    arc154=$arc_actuel
                    personnage154=$raiponce
                    
                else
                    if [[ $nombre155 -eq 0 && $arc155 -ne $arc_actuel ]]; then
                        nombre155=$ran_a
                        arc155=$arc_actuel
                        personnage155=$raiponce
                        
                    else
                        installation160
                    fi
                fi
            fi
        fi
    fi
}

installation150() {
    if [[ $nombre146 -eq 0 && $arc146 -ne $arc_actuel ]]; then
        nombre146=$ran_a
        arc146=$arc_actuel
        personnage146=$raiponce
        
    else
        if [[ $nombre147 -eq 0 && $arc147 -ne $arc_actuel ]]; then
            nombre147=$ran_a
            arc147=$arc_actuel
            personnage147=$raiponce
            
        else
            if [[ $nombre148 -eq 0 && $arc148 -ne $arc_actuel ]]; then
                nombre148=$ran_a
                arc148=$arc_actuel
                personnage148=$raiponce
                
            else
                if [[ $nombre149 -eq 0 && $arc149 -ne $arc_actuel ]]; then
                    nombre149=$ran_a
                    arc149=$arc_actuel
                    personnage149=$raiponce
                    
                else
                    if [[ $nombre150 -eq 0 && $arc150 -ne $arc_actuel ]]; then
                        nombre150=$ran_a
                        arc150=$arc_actuel
                        personnage150=$raiponce
                        
                    else
                        installation155
                    fi
                fi
            fi
        fi
    fi
}

installation145() {
    if [[ $nombre141 -eq 0 && $arc141 -ne $arc_actuel ]]; then
        nombre141=$ran_a
        arc141=$arc_actuel
        personnage141=$raiponce
        
    else
        if [[ $nombre142 -eq 0 && $arc142 -ne $arc_actuel ]]; then
            nombre142=$ran_a
            arc142=$arc_actuel
            personnage142=$raiponce
            
        else
            if [[ $nombre143 -eq 0 && $arc143 -ne $arc_actuel ]]; then
                nombre143=$ran_a
                arc143=$arc_actuel
                personnage143=$raiponce
                
            else
                if [[ $nombre144 -eq 0 && $arc144 -ne $arc_actuel ]]; then
                    nombre144=$ran_a
                    arc144=$arc_actuel
                    personnage144=$raiponce
                    
                else
                    if [[ $nombre145 -eq 0 && $arc145 -ne $arc_actuel ]]; then
                        nombre145=$ran_a
                        arc145=$arc_actuel
                        personnage145=$raiponce
                        
                    else
                        installation150
                    fi
                fi
            fi
        fi
    fi
}

installation140() {
    if [[ $nombre136 -eq 0 && $arc136 -ne $arc_actuel ]]; then
        nombre136=$ran_a
        arc136=$arc_actuel
        personnage136=$raiponce
        
    else
        if [[ $nombre137 -eq 0 && $arc137 -ne $arc_actuel ]]; then
            nombre137=$ran_a
            arc137=$arc_actuel
            personnage137=$raiponce
            
        else
            if [[ $nombre138 -eq 0 && $arc138 -ne $arc_actuel ]]; then
                nombre138=$ran_a
                arc138=$arc_actuel
                personnage138=$raiponce
                
            else
                if [[ $nombre139 -eq 0 && $arc139 -ne $arc_actuel ]]; then
                    nombre139=$ran_a
                    arc139=$arc_actuel
                    personnage139=$raiponce
                    
                else
                    if [[ $nombre140 -eq 0 && $arc140 -ne $arc_actuel ]]; then
                        nombre140=$ran_a
                        arc140=$arc_actuel
                        personnage140=$raiponce
                        
                    else
                        installation145
                    fi
                fi
            fi
        fi
    fi
}

installation135() {
    if [[ $nombre131 -eq 0 && $arc131 -ne $arc_actuel ]]; then
        nombre131=$ran_a
        arc131=$arc_actuel
        personnage131=$raiponce
        
    else
        if [[ $nombre132 -eq 0 && $arc132 -ne $arc_actuel ]]; then
            nombre132=$ran_a
            arc132=$arc_actuel
            personnage132=$raiponce
            
        else
            if [[ $nombre133 -eq 0 && $arc133 -ne $arc_actuel ]]; then
                nombre133=$ran_a
                arc133=$arc_actuel
                personnage133=$raiponce
                
            else
                if [[ $nombre134 -eq 0 && $arc134 -ne $arc_actuel ]]; then
                    nombre134=$ran_a
                    arc134=$arc_actuel
                    personnage134=$raiponce
                    
                else
                    if [[ $nombre135 -eq 0 && $arc135 -ne $arc_actuel ]]; then
                        nombre135=$ran_a
                        arc135=$arc_actuel
                        personnage135=$raiponce
                        
                    else
                        installation140
                    fi
                fi
            fi
        fi
    fi
}

installation130() {
    if [[ $nombre126 -eq 0 && $arc126 -ne $arc_actuel ]]; then
        nombre126=$ran_a
        arc126=$arc_actuel
        personnage126=$raiponce
        
    else
        if [[ $nombre127 -eq 0 && $arc127 -ne $arc_actuel ]]; then
            nombre127=$ran_a
            arc127=$arc_actuel
            personnage127=$raiponce
            
        else
            if [[ $nombre128 -eq 0 && $arc128 -ne $arc_actuel ]]; then
                nombre128=$ran_a
                arc128=$arc_actuel
                personnage128=$raiponce
                
            else
                if [[ $nombre129 -eq 0 && $arc129 -ne $arc_actuel ]]; then
                    nombre129=$ran_a
                    arc129=$arc_actuel
                    personnage129=$raiponce
                    
                else
                    if [[ $nombre130 -eq 0 && $arc130 -ne $arc_actuel ]]; then
                        nombre130=$ran_a
                        arc130=$arc_actuel
                        personnage130=$raiponce
                        
                    else
                        installation135
                    fi
                fi
            fi
        fi
    fi
}

installation125() {
    if [[ $nombre121 -eq 0 && $arc121 -ne $arc_actuel ]]; then
        nombre121=$ran_a
        arc121=$arc_actuel
        personnage121=$raiponce
        
    else
        if [[ $nombre122 -eq 0 && $arc122 -ne $arc_actuel ]]; then
            nombre122=$ran_a
            arc122=$arc_actuel
            personnage122=$raiponce
            
        else
            if [[ $nombre123 -eq 0 && $arc123 -ne $arc_actuel ]]; then
                nombre123=$ran_a
                arc123=$arc_actuel
                personnage123=$raiponce
                
            else
                if [[ $nombre124 -eq 0 && $arc124 -ne $arc_actuel ]]; then
                    nombre124=$ran_a
                    arc124=$arc_actuel
                    personnage124=$raiponce
                    
                else
                    if [[ $nombre125 -eq 0 && $arc125 -ne $arc_actuel ]]; then
                        nombre125=$ran_a
                        arc125=$arc_actuel
                        personnage125=$raiponce
                        
                    else
                        installation130
                    fi
                fi
            fi
        fi
    fi
}

installation120() {
    if [[ $nombre116 -eq 0 && $arc116 -ne $arc_actuel ]]; then
        nombre116=$ran_a
        arc116=$arc_actuel
        personnage116=$raiponce
        
    else
        if [[ $nombre117 -eq 0 && $arc117 -ne $arc_actuel ]]; then
            nombre117=$ran_a
            arc117=$arc_actuel
            personnage117=$raiponce
            
        else
            if [[ $nombre118 -eq 0 && $arc118 -ne $arc_actuel ]]; then
                nombre118=$ran_a
                arc118=$arc_actuel
                personnage118=$raiponce
                
            else
                if [[ $nombre119 -eq 0 && $arc119 -ne $arc_actuel ]]; then
                    nombre119=$ran_a
                    arc119=$arc_actuel
                    personnage119=$raiponce
                    
                else
                    if [[ $nombre120 -eq 0 && $arc120 -ne $arc_actuel ]]; then
                        nombre120=$ran_a
                        arc120=$arc_actuel
                        personnage120=$raiponce
                        
                    else
                        installation125
                    fi
                fi
            fi
        fi
    fi
} 

installation115() {
    if [[ $nombre111 -eq 0 && $arc111 -ne $arc_actuel ]]; then
        nombre111=$ran_a
        arc111=$arc_actuel
        personnage111=$raiponce
        
    else
        if [[ $nombre112 -eq 0 && $arc112 -ne $arc_actuel ]]; then
            nombre112=$ran_a
            arc112=$arc_actuel
            personnage112=$raiponce
            
        else
            if [[ $nombre113 -eq 0 && $arc113 -ne $arc_actuel ]]; then
                nombre113=$ran_a
                arc113=$arc_actuel
                personnage113=$raiponce
                
            else
                if [[ $nombre114 -eq 0 && $arc114 -ne $arc_actuel ]]; then
                    nombre114=$ran_a
                    arc114=$arc_actuel
                    personnage114=$raiponce
                    
                else
                    if [[ $nombre115 -eq 0 && $arc115 -ne $arc_actuel ]]; then
                        nombre115=$ran_a
                        arc115=$arc_actuel
                        personnage115=$raiponce
                        
                    else
                        installation120
                    fi
                fi
            fi
        fi
    fi
}

installation110() {
    if [[ $nombre106 -eq 0 && $arc106 -ne $arc_actuel ]]; then
        nombre106=$ran_a
        arc106=$arc_actuel
        personnage106=$raiponce
        
    else
        if [[ $nombre107 -eq 0 && $arc107 -ne $arc_actuel ]]; then
            nombre107=$ran_a
            arc107=$arc_actuel
            personnage107=$raiponce
            
        else
            if [[ $nombre108 -eq 0 && $arc108 -ne $arc_actuel ]]; then
                nombre108=$ran_a
                arc108=$arc_actuel
                personnage108=$raiponce
                
            else
                if [[ $nombre109 -eq 0 && $arc109 -ne $arc_actuel ]]; then
                    nombre109=$ran_a
                    arc109=$arc_actuel
                    personnage109=$raiponce
                    
                else
                    if [[ $nombre110 -eq 0 && $arc110 -ne $arc_actuel ]]; then
                        nombre110=$ran_a
                        arc110=$arc_actuel
                        personnage110=$raiponce
                        
                    else
                        installation115
                    fi
                fi
            fi
        fi
    fi
}


installation105() {
    if [[ $nombre101 -eq 0 && $arc101 -ne $arc_actuel ]]; then
        nombre101=$ran_a
        arc101=$arc_actuel
        personnage101=$raiponce
        
    else
        if [[ $nombre102 -eq 0 && $arc102 -ne $arc_actuel ]]; then
            nombre102=$ran_a
            arc102=$arc_actuel
            personnage102=$raiponce
            
        else
            if [[ $nombre103 -eq 0 && $arc103 -ne $arc_actuel ]]; then
                nombre103=$ran_a
                arc103=$arc_actuel
                personnage103=$raiponce
                
            else
                if [[ $nombre104 -eq 0 && $arc104 -ne $arc_actuel ]]; then
                    nombre104=$ran_a
                    arc104=$arc_actuel
                    personnage104=$raiponce
                    
                else
                    if [[ $nombre105 -eq 0 && $arc105 -ne $arc_actuel ]]; then
                        nombre105=$ran_a
                        arc105=$arc_actuel
                        personnage105=$raiponce
                        
                    else
                        installation110
                    fi
                fi
            fi
        fi
    fi
}

installation100() {
    if [[ $nombre96 -eq 0 && $arc96 -ne $arc_actuel ]]; then
        nombre96=$ran_a
        arc96=$arc_actuel
        personnage96=$raiponce
        
    else
        if [[ $nombre97 -eq 0 && $arc97 -ne $arc_actuel ]]; then
            nombre97=$ran_a
            arc97=$arc_actuel
            personnage97=$raiponce
            
        else
            if [[ $nombre98 -eq 0 && $arc98 -ne $arc_actuel ]]; then
                nombre98=$ran_a
                arc98=$arc_actuel
                personnage98=$raiponce
                
            else
                if [[ $nombre99 -eq 0 && $arc99 -ne $arc_actuel ]]; then
                    nombre99=$ran_a
                    arc99=$arc_actuel
                    personnage99=$raiponce
                    
                else
                    if [[ $nombre100 -eq 0 && $arc100 -ne $arc_actuel ]]; then
                        nombre100=$ran_a
                        arc100=$arc_actuel
                        personnage100=$raiponce
                        
                    else
                        installation105
                    fi
                fi
            fi
        fi
    fi
}

installation95() {
    if [[ $nombre91 -eq 0 && $arc91 -ne $arc_actuel ]]; then
        nombre91=$ran_a
        arc91=$arc_actuel
        personnage91=$raiponce
        
    else
        if [[ $nombre92 -eq 0 && $arc92 -ne $arc_actuel ]]; then
            nombre92=$ran_a
            arc92=$arc_actuel
            personnage92=$raiponce
            
        else
            if [[ $nombre93 -eq 0 && $arc93 -ne $arc_actuel ]]; then
                nombre93=$ran_a
                arc93=$arc_actuel
                personnage93=$raiponce
                
            else
                if [[ $nombre94 -eq 0 && $arc94 -ne $arc_actuel ]]; then
                    nombre94=$ran_a
                    arc94=$arc_actuel
                    personnage94=$raiponce
                    
                else
                    if [[ $nombre95 -eq 0 && $arc95 -ne $arc_actuel ]]; then
                        nombre95=$ran_a
                        arc95=$arc_actuel
                        personnage95=$raiponce
                        
                    else
                        installation100
                    fi
                fi
            fi
        fi
    fi
}

installation90() {
    if [[ $nombre86 -eq 0 && $arc86 -ne $arc_actuel ]]; then
        nombre86=$ran_a
        arc86=$arc_actuel
        personnage86=$raiponce
        
    else
        if [[ $nombre87 -eq 0 && $arc87 -ne $arc_actuel ]]; then
            nombre87=$ran_a
            arc87=$arc_actuel
            personnage87=$raiponce
            
        else
            if [[ $nombre88 -eq 0 && $arc88 -ne $arc_actuel ]]; then
                nombre88=$ran_a
                arc88=$arc_actuel
                personnage88=$raiponce
                
            else
                if [[ $nombre89 -eq 0 && $arc89 -ne $arc_actuel ]]; then
                    nombre89=$ran_a
                    arc89=$arc_actuel
                    personnage89=$raiponce
                    
                else
                    if [[ $nombre90 -eq 0 && $arc90 -ne $arc_actuel ]]; then
                        nombre90=$ran_a
                        arc90=$arc_actuel
                        personnage90=$raiponce
                        
                    else
                        installation95
                    fi
                fi
            fi
        fi
    fi
}

installation85() {
    if [[ $nombre81 -eq 0 && $arc81 -ne $arc_actuel ]]; then
        nombre81=$ran_a
        arc81=$arc_actuel
        personnage81=$raiponce
        
    else
        if [[ $nombre82 -eq 0 && $arc82 -ne $arc_actuel ]]; then
            nombre82=$ran_a
            arc82=$arc_actuel
            personnage82=$raiponce
            
        else
            if [[ $nombre83 -eq 0 && $arc83 -ne $arc_actuel ]]; then
                nombre83=$ran_a
                arc83=$arc_actuel
                personnage83=$raiponce
                
            else
                if [[ $nombre84 -eq 0 && $arc84 -ne $arc_actuel ]]; then
                    nombre84=$ran_a
                    arc84=$arc_actuel
                    personnage84=$raiponce
                    
                else
                    if [[ $nombre85 -eq 0 && $arc85 -ne $arc_actuel ]]; then
                        nombre85=$ran_a
                        arc85=$arc_actuel
                        personnage85=$raiponce
                        
                    else
                        installation90
                    fi
                fi
            fi
        fi
    fi
}

installation80() {
    if [[ $nombre76 -eq 0 && $arc76 -ne $arc_actuel ]]; then
        nombre76=$ran_a
        arc76=$arc_actuel
        personnage76=$raiponce
        
    else
        if [[ $nombre77 -eq 0 && $arc77 -ne $arc_actuel ]]; then
            nombre77=$ran_a
            arc77=$arc_actuel
            personnage77=$raiponce
            
        else
            if [[ $nombre78 -eq 0 && $arc78 -ne $arc_actuel  ]]; then
                nombre78=$ran_a
                arc78=$arc_actuel
                personnage78=$raiponce
                
            else
                if [[ $nombre79 -eq 0 && $arc79 -ne $arc_actuel ]]; then
                    nombre79=$ran_a
                    arc79=$arc_actuel
                    personnage79=$raiponce
                    
                else
                    if [[ $nombre80 -eq 0 && $arc80 -ne $arc_actuel ]]; then
                        nombre80=$ran_a
                        arc80=$arc_actuel
                        personnage80=$raiponce
                        
                    else
                        installation85
                    fi
                fi
            fi
        fi
    fi
}

installation75() {
    if [[ $nombre71 -eq 0 && $arc71 -ne $arc_actuel ]]; then
        nombre71=$ran_a
        arc71=$arc_actuel
        personnage71=$raiponce
        
    else
        if [[ $nombre72 -eq 0 && $arc72 -ne $arc_actuel ]]; then
            nombre72=$ran_a
            arc72=$arc_actuel
            personnage72=$raiponce
            
        else
            if [[ $nombre73 -eq 0 && $arc73 -ne $arc_actuel ]]; then
                nombre73=$ran_a
                arc73=$arc_actuel
                personnage73=$raiponce
                
            else
                if [[ $nombre74 -eq 0 && $arc74 -ne $arc_actuel ]]; then
                    nombre74=$ran_a
                    arc74=$arc_actuel
                    personnage74=$raiponce
                    
                else
                    if [[ $nombre75 -eq 0 && $arc75 -ne $arc_actuel ]]; then
                        nombre75=$ran_a
                        arc75=$arc_actuel
                        personnage75=$raiponce
                        
                    else
                        installation80
                    fi
                fi
            fi
        fi
    fi
}

installation70() {
    if [[ $nombre66 -eq 0 && $arc66 -ne $arc_actuel ]]; then
        nombre66=$ran_a
        arc66=$arc_actuel
        personnage66=$raiponce
        
    else
        if [[ $nombre67 -eq 0 && $arc67 -ne $arc_actuel ]]; then
            nombre67=$ran_a
            arc67=$arc_actuel
            personnage67=$raiponce
            
        else
            if [[ $nombre68 -eq 0 && $arc68 -ne $arc_actuel ]]; then
                nombre68=$ran_a
                arc68=$arc_actuel
                personnage68=$raiponce
                
            else
                if [[ $nombre69 -eq 0 && $arc69 -ne $arc_actuel ]]; then
                    nombre69=$ran_a
                    arc69=$arc_actuel
                    personnage69=$raiponce
                    
                else
                    if [[ $nombre70 -eq 0 && $arc70 -ne $arc_actuel ]]; then
                        nombre70=$ran_a
                        arc70=$arc_actuel
                        personnage70=$raiponce
                        
                    else
                        installation75
                    fi
                fi
            fi
        fi
    fi
}

installation65() {
    if [[ $nombre61 -eq 0 && $arc61 -ne $arc_actuel ]]; then
        nombre61=$ran_a
        arc61=$arc_actuel
        personnage61=$raiponce
        
    else
        if [[ $nombre62 -eq 0 && $arc62 -ne $arc_actuel ]]; then
            nombre62=$ran_a
            arc62=$arc_actuel
            personnage62=$raiponce
            
        else
            if [[ $nombre63 -eq 0 && $arc63 -ne $arc_actuel ]]; then
                nombre63=$ran_a
                arc63=$arc_actuel
                personnage63=$raiponce
                
            else
                if [[ $nombre64 -eq 0 && $arc64 -ne $arc_actuel ]]; then
                    nombre64=$ran_a
                    arc64=$arc_actuel
                    personnage64=$raiponce
                    
                else
                    if [[ $nombre65 -eq 0 && $arc65 -ne $arc_actuel ]]; then
                        nombre65=$ran_a
                        arc65=$arc_actuel
                        personnage65=$raiponce
                        
                    else
                        installation70
                    fi
                fi
            fi
        fi
    fi
}

installation60() {
    if [[ $nombre56 -eq 0 && $arc56 -ne $arc_actuel ]]; then
        nombre56=$ran_a
        arc56=$arc_actuel
        personnage56=$raiponce
        
    else
        if [[ $nombre57 -eq 0 && $arc57 -ne $arc_actuel ]]; then
            nombre57=$ran_a
            arc57=$arc_actuel
            personnage57=$raiponce
            
        else
            if [[ $nombre58 -eq 0 && $arc58 -ne $arc_actuel ]]; then
                nombre58=$ran_a
                arc58=$arc_actuel
                personnage58=$raiponce
                
            else
                if [[ $nombre59 -eq 0 && $arc59 -ne $arc_actuel ]]; then
                    nombre59=$ran_a
                    arc59=$arc_actuel
                    personnage59=$raiponce
                    
                else
                    if [[ $nombre60 -eq 0 && $arc60 -ne $arc_actuel ]]; then
                        nombre60=$ran_a
                        arc60=$arc_actuel
                        personnage60=$raiponce
                        
                    else
                        installation65
                    fi
                fi
            fi
        fi
    fi
}

installation55() {
    if [[ $nombre51 -eq 0 && $arc51 -ne $arc_actuel ]]; then
        nombre51=$ran_a
        arc51=$arc_actuel
        personnage51=$raiponce
        
    else
        if [[ $nombre52 -eq 0 && $arc52 -ne $arc_actuel ]]; then
            nombre52=$ran_a
            arc52=$arc_actuel
            personnage52=$raiponce
            
        else
            if [[ $nombre53 -eq 0 && $arc53 -ne $arc_actuel ]]; then
                nombre53=$ran_a
                arc53=$arc_actuel
                personnage53=$raiponce
                
            else
                if [[ $nombre54 -eq 0 && $arc54 -ne $arc_actuel ]]; then
                    nombre54=$ran_a
                    arc54=$arc_actuel
                    personnage54=$raiponce
                    
                else
                    if [[ $nombre55 -eq 0 && $arc55 -ne $arc_actuel ]]; then
                        nombre55=$ran_a
                        arc55=$arc_actuel
                        personnage55=$raiponce
                        
                    else
                        installation60
                    fi
                fi
            fi
        fi
    fi
}

installation50() {
    if [[ $nombre46 -eq 0 && $arc46 -ne $arc_actuel ]]; then
        nombre46=$ran_a
        arc46=$arc_actuel
        personnage46=$raiponce
        
    else
        if [[ $nombre47 -eq 0 && $arc47 -ne $arc_actuel ]]; then
            nombre47=$ran_a
            arc47=$arc_actuel
            personnage47=$raiponce
            
        else
            if [[ $nombre48 -eq 0 && $arc48 -ne $arc_actuel ]]; then
                nombre48=$ran_a
                arc48=$arc_actuel
                personnage48=$raiponce
                
            else
                if [[ $nombre49 -eq 0 && $arc49 -ne $arc_actuel ]]; then
                    nombre49=$ran_a
                    arc49=$arc_actuel
                    personnage49=$raiponce
                    
                else
                    if [[ $nombre50 -eq 0 && $arc50 -ne $arc_actuel ]]; then
                        nombre50=$ran_a
                        arc50=$arc_actuel
                        personnage50=$raiponce
                        
                    else
                        installation55
                    fi
                fi
            fi
        fi
    fi
}

installation45() {
    if [[ $nombre41 -eq 0 && $arc41 -ne $arc_actuel ]]; then
        nombre41=$ran_a
        arc41=$arc_actuel
        personnage41=$raiponce
        
    else
        if [[ $nombre42 -eq 0 && $arc42 -ne $arc_actuel ]]; then
            nombre42=$ran_a
            arc42=$arc_actuel
            personnage42=$raiponce
            
        else
            if [[ $nombre43 -eq 0 && $arc43 -ne $arc_actuel ]]; then
                nombre43=$ran_a
                arc43=$arc_actuel
                personnage43=$raiponce
                
            else
                if [[ $nombre44 -eq 0 && $arc44 -ne $arc_actuel ]]; then
                    nombre44=$ran_a
                    arc44=$arc_actuel
                    personnage44=$raiponce
                    
                else
                    if [[ $nombre45 -eq 0 && $arc45 -ne $arc_actuel ]]; then
                        nombre45=$ran_a
                        arc45=$arc_actuel
                        personnage45=$raiponce
                        
                    else
                        installation50
                    fi
                fi
            fi
        fi
    fi
}

installation40() {
    if [[ $nombre36 -eq 0 && $arc36 -ne $arc_actuel ]]; then
        nombre36=$ran_a
        arc36=$arc_actuel
        personnage36=$raiponce
        
    else
        if [[ $nombre37 -eq 0 && $arc37 -ne $arc_actuel ]]; then
            nombre37=$ran_a
            arc37=$arc_actuel
            personnage37=$raiponce
            
        else
            if [[ $nombre38 -eq 0 && $arc38 -ne $arc_actuel ]]; then
                nombre38=$ran_a
                arc38=$arc_actuel
                personnage38=$raiponce
                
            else
                if [[ $nombre39 -eq 0 && $arc39 -ne $arc_actuel ]]; then
                    nombre39=$ran_a
                    arc39=$arc_actuel
                    personnage39=$raiponce
                    
                else
                    if [[ $nombre40 -eq 0 && $arc40 -ne $arc_actuel ]]; then
                        nombre40=$ran_a
                        arc40=$arc_actuel
                        personnage40=$raiponce
                        
                    else
                        installation45
                    fi
                fi
            fi
        fi
    fi
}

installation35() {
    if [[ $nombre31 -eq 0 && $arc31 -ne $arc_actuel ]]; then
        nombre31=$ran_a
        arc31=$arc_actuel
        personnage31=$raiponce
        
    else
        if [[ $nombre32 -eq 0 && $arc32 -ne $arc_actuel ]]; then
            nombre32=$ran_a
            arc32=$arc_actuel
            personnage32=$raiponce
            
        else
            if [[ $nombre33 -eq 0 && $arc33 -ne $arc_actuel ]]; then
                nombre33=$ran_a
                arc33=$arc_actuel
                personnage33=$raiponce
                
            else
                if [[ $nombre34 -eq 0 && $arc34 -ne $arc_actuel ]]; then
                    nombre34=$ran_a
                    arc34=$arc_actuel
                    personnage34=$raiponce
                    
                else
                    if [[ $nombre35 -eq 0 && $arc35 -ne $arc_actuel ]]; then
                        nombre35=$ran_a
                        arc35=$arc_actuel
                        personnage35=$raiponce
                        
                    else
                        installation40
                    fi
                fi
            fi
        fi
    fi
}

installation30() {
    if [[ $nombre26 -eq 0 && $arc26 -ne $arc_actuel ]]; then
        nombre26=$ran_a
        arc26=$arc_actuel
        personnage26=$raiponce
        
    else
        if [[ $nombre27 -eq 0 && $arc27 -ne $arc_actuel ]]; then
            nombre27=$ran_a
            arc27=$arc_actuel
            personnage27=$raiponce
            
        else
            if [[ $nombre28 -eq 0 && $arc28 -ne $arc_actuel ]]; then
                nombre28=$ran_a
                arc28=$arc_actuel
                personnage28=$raiponce
                
            else
                if [[ $nombre29 -eq 0 && $arc29 -ne $arc_actuel ]]; then
                    nombre29=$ran_a
                    arc29=$arc_actuel
                    personnage29=$raiponce
                    
                else
                    if [[ $nombre30 -eq 0 && $arc30 -ne $arc_actuel ]]; then
                        nombre30=$ran_a
                        arc30=$arc_actuel
                        personnage30=$raiponce
                        
                    else
                        installation35
                    fi
                fi
            fi
        fi
    fi
}

installation25() {
    if [[ $nombre21 -eq 0 && $arc21 -ne $arc_actuel ]]; then
        nombre21=$ran_a
        arc21=$arc_actuel
        personnage21=$raiponce
        
    else
        if [[ $nombre22 -eq 0 && $arc22 -ne $arc_actuel ]]; then
            nombre22=$ran_a
            arc22=$arc_actuel
            personnage22=$raiponce
            
        else
            if [[ $nombre23 -eq 0 && $arc23 -ne $arc_actuel ]]; then
                nombre23=$ran_a
                arc23=$arc_actuel
                personnage23=$raiponce
                
            else
                if [[ $nombre24 -eq 0 && $arc24 -ne $arc_actuel ]]; then
                    nombre24=$ran_a
                    arc24=$arc_actuel
                    personnage24=$raiponce
                    
                else
                    if [[ $nombre25 -eq 0 && $arc25 -ne $arc_actuel ]]; then
                        nombre25=$ran_a
                        arc25=$arc_actuel
                        personnage25=$raiponce
                        
                    else
                        installation30
                    fi
                fi
            fi
        fi
    fi
}

installation20() {
    if [[ $nombre16 -eq 0 && $arc16 -ne $arc_actuel ]]; then
        nombre16=$ran_a
        arc16=$arc_actuel
        personnage16=$raiponce
        
    else
        if [[ $nombre17 -eq 0 && $arc17 -ne $arc_actuel ]]; then
            nombre17=$ran_a
            arc17=$arc_actuel
            personnage17=$raiponce
            
        else
            if [[ $nombre18 -eq 0 && $arc18 -ne $arc_actuel ]]; then
                nombre18=$ran_a
                arc18=$arc_actuel
                personnage18=$raiponce
                
            else
                if [[ $nombre19 -eq 0 && $arc19 -ne $arc_actuel ]]; then
                    nombre19=$ran_a
                    arc19=$arc_actuel
                    personnage19=$raiponce
                    
                else
                    if [[ $nombre20 -eq 0 && $arc20 -ne $arc_actuel ]]; then
                        nombre20=$ran_a
                        arc20=$arc_actuel
                        personnage20=$raiponce
                        
                    else
                        installation25
                    fi
                fi
            fi
        fi
    fi
} 

installation15() {
    if [[ $nombre11 -eq 0 && $arc11 -ne $arc_actuel ]]; then
        nombre11=$ran_a
        arc11=$arc_actuel
        personnage11=$raiponce
        
    else
        if [[ $nombre12 -eq 0 && $arc12 -ne $arc_actuel ]]; then
            nombre12=$ran_a
            arc12=$arc_actuel
            personnage12=$raiponce
            
        else
            if [[ $nombre13 -eq 0 && $arc13 -ne $arc_actuel ]]; then
                nombre13=$ran_a
                arc13=$arc_actuel
                personnage13=$raiponce
                
            else
                if [[ $nombre14 -eq 0 && $arc14 -ne $arc_actuel ]]; then
                    nombre14=$ran_a
                    arc14=$arc_actuel
                    personnage14=$raiponce
                    
                else
                    if [[ $nombre15 -eq 0 && $arc15 -ne $arc_actuel ]]; then
                        nombre15=$ran_a
                        arc15=$arc_actuel
                        personnage15=$raiponce
                        
                    else
                        installation20
                    fi
                fi
            fi
        fi
    fi
}

installation10() {
    if [[ $nombre6 -eq 0 && $arc6 -ne $arc_actuel ]]; then
        nombre6=$ran_a
        arc6=$arc_actuel
        personnage6=$raiponce        
    else
        if [[ $nombre7 -eq 0 && $arc7 -ne $arc_actuel ]]; then
            nombre7=$ran_a
            arc7=$arc_actuel
            personnage7=$raiponce            
        else
            if [[ $nombre8 -eq 0 && $arc8 -ne $arc_actuel ]]; then
                nombre8=$ran_a
                arc8=$arc_actuel
                personnage8=$raiponce                
            else
                if [[ $nombre9 -eq 0 && $arc9 -ne $arc_actuel ]]; then
                    nombre9=$ran_a
                    arc9=$arc_actuel
                    personnage9=$raiponce                    
                else
                    if [[ $nombre10 -eq 0 && $arc10 -ne $arc_actuel ]]; then
                        nombre10=$ran_a
                        arc10=$arc_actuel
                        personnage10=$raiponce
                        
                    else
                        installation15
                    fi
                fi
            fi
        fi
    fi
}

installation5() {
    if [[ $nombre1 -eq 0 && $arc1 -ne $arc_actuel ]]; then
        nombre1=$ran_a
        arc1=$arc_actuel
        personnage1=$raiponce        
    else
        if [[ $nombre2 -eq 0 && $arc2 -ne $arc_actuel ]]; then
            nombre2=$ran_a
            arc2=$arc_actuel
            personnage2=$raiponce            
        else
            if [[ $nombre3 -eq 0 && $arc3 -ne $arc_actuel ]]; then
                nombre3=$ran_a
                arc3=$arc_actuel
                personnage3=$raiponce                
            else
                if [[ $nombre4 -eq 0 && $arc4 -ne $arc_actuel ]]; then
                    nombre4=$ran_a
                    arc4=$arc_actuel
                    personnage4=$raiponce                    
                else
                    if [[ $nombre5 -eq 0 && $arc5 -ne $arc_actuel ]]; then
                        nombre5=$ran_a
                        arc5=$arc_actuel
                        personnage5=$raiponce   
                    else
                        installation10
                    fi
                fi
            fi
        fi
    fi
}

installation5
